# 模型蒸馏专题学习计划

> *复用主计划已经建立的 Qwen 训练资产，用一条轻量、可复现的流水线完成序列级蒸馏。*

---

## 目录

1. [专题定位](#一专题定位)
2. [研究问题与技术路线](#二研究问题与技术路线)
3. [时间安排](#三时间安排)
4. [阶段执行计划](#四阶段执行计划)
5. [实验与评估规范](#五实验与评估规范)
6. [项目里程碑](#六项目里程碑)
7. [条件式扩展](#七条件式扩展)
8. [结项标准](#八结项标准)

---

## 一、专题定位

### 1.1　计划目标

本计划承接 [LLM 自学项目计划](./LLM自学计划.md) 的阶段 10。在 Qwen3.5 Base、9B QLoRA SFT、TRL 数据流和 LightEval 评估已经跑通后，集中学习以下内容：

- Teacher 多候选数据的批量生成与自动验证；
- Answer-only、Compressed rationale 与 Full reasoning 三类序列级监督；
- 使用 TRL 完成正式的 SFT 蒸馏；
- 在统一口径下比较正确率、输出长度和推理成本；

本专题以熟悉算法、框架和研究流程为目标。核心路线保留每种蒸馏信号的第一次完整实践，减少多模型重复训练、数据平台开发和论文级稳健性验证。

正式数据直接采用公开 benchmark、官方答案和现成评估工具。学习者不编写题目、不制作标签、不逐条清洗。无法解析、输出截断或重复的候选不进入派生数据；可解析但答案错误的候选保留在原始候选池中。

### 1.2　直接继承的项目资产

进入专题前，以下产物应已由主计划生成并冻结，本专题直接读取，不重新制作：

| 资产 | 继承内容 |
| --- | --- |
| 数据基线 | 阶段 8 生成、阶段 10 核验的 `gsm8k_split_v1.json`，包含固定 train/development、`smoke_100`、样本 ID 与 revision |
| 泛化评估 | MATH-500 固定 revision，只读 |
| 通用回归 | IFEval、MMLU 与 TruthfulQA 的冻结 LightEval 任务配置和 Base / SFT 结果 |
| 模型基线 | Qwen3.5-9B Base 与普通 GSM8K QLoRA SFT 结果 |
| 训练规范 | TRL YAML、Accelerate 启动配置、TensorBoard 记录方式 |
| 评估规范 | Math-Verify、LightEval task、generation config 与结果目录 |
| 版本规范 | Git commit、模型 revision、Dataset fingerprint 与 checkpoint 索引 |

### 1.3　前置基础

进入本计划前，应能够：

- 解释交叉熵、KL 散度、SFT 与 sequence-level distillation；
- 使用 Transformers、Datasets、PEFT、TRL 和 Accelerate；
- 加载 Qwen Base、adapter 和合并后的 checkpoint；
- 使用 vLLM 或 SGLang 批量生成；
- 使用 Math-Verify 与 LightEval 完成独立评估；
- 读取已有 YAML、日志、Dataset fingerprint 和 checkpoint 记录。

MiniMind 阶段已经完成同 tokenizer 的 logit KD。本专题不重复 logit distillation，也不重新实现训练循环或分布式训练器。

### 1.4　时间与资源

| 项目 | 安排 |
| --- | --- |
| 每周投入 | 35 小时 |
| 每周新内容 / 第三天复习 | 约 27 小时 / 约 8 小时 |
| 核心周期 | 2.5～3 周 |
| 计划窗口 | 第 7 周～第 10 周前半 |
| 核心投入 | 约 76～104 小时 |
| 计算资源 | 租用多卡高算力服务器 |
| 首选 Teacher | `openai/gpt-oss-20b` |
| 备选 Teacher | `Qwen/Qwen3.8-27B` |
| 开发 Student | `Qwen/Qwen3.5-2B-Base` |
| 主实验 Student | `Qwen/Qwen3.5-9B-Base` |
| 模型与数据平台 | Hugging Face |

每周投入只计算需要持续参与的学习、配置、检查、分析和记录时间。批量生成与训练的无人值守等待时间单独记录。每天完成新内容后，在第三天（学习日记为 D 时的 D+2）安排约为首次学习时间 30% 的复习；复习时间已经计入阶段总投入和每周 35 小时。复习以脱离配置说明数据流、复查一项 verifier 或 mask 结果、重新运行一个关键入口为主。

阶段运行完成后可以继续启动后续流水线，对应第三天复习完成后才关闭阶段里程碑。阶段 D4 完成后使用实际主动投入校准结项工作；偏离预算超过 20% 时才调整尚未启动的工作，不扩大正式实验矩阵。

正式矩阵只使用一个 Teacher。D1 开始前根据服务器可用性、部署稳定性和生成吞吐，在 `gpt-oss-20b` 与 `Qwen3.8-27B` 中选定一个；选定后不混用候选数据。多随机种子属于条件式扩展。新增算力优先缩短迭代等待，不用于无条件扩大实验矩阵。

本专题完成后进入阶段 11 的 RL 系统学习。阶段 12 的架构研究在 RL 专题结项后重新冻结研究问题，不预先锁死为 GQA 路线。

### 1.5　专题主线

```text
继承主计划 baseline
→ Open R1 / Distilabel 生成 Teacher 候选
→ Harmony 解析与 Math-Verify 自动验证
→ 派生三类序列监督
→ 2B 短流程调试
→ 9B 正式监督形式对照
→ 一份分析 notebook 与专题总结
```

---

## 二、研究问题与技术路线

### 2.1　核心研究问题

专题只回答一个核心问题：

> 在固定 Teacher、公开任务与 Student 训练预算下，Answer-only、Compressed rationale 和 Full reasoning 对 Qwen3.5-9B 的正确率、输出长度与推理成本产生怎样的影响？

核心变量是 Teacher 提供的序列监督形式。

### 2.2　固定数据与评测基线

| 用途 | 数据集 | 固定用法 |
| --- | --- | --- |
| Teacher prompts 与正式训练 | [`openai/gsm8k`](https://huggingface.co/datasets/openai/gsm8k)，config `main` | `gsm8k_split_v1.json` 中冻结的 6,973 条 train prompts |
| Development | GSM8K train 子集 | 同一清单中的 500 条固定 row ID |
| 主评估 | GSM8K 官方 test | 1,319 条，只读 |
| 泛化评估 | [`HuggingFaceH4/MATH-500`](https://huggingface.co/datasets/HuggingFaceH4/MATH-500) | 500 条，只读 |
| 指令遵循回归 | IFEval | 继承主计划冻结的 LightEval task，只读 |
| 通用能力回归 | MMLU | 继承主计划冻结的 LightEval task，只读 |
| 真实性风险信号 | TruthfulQA multiple-choice | 继承主计划冻结的 LightEval task，只读 |

GSM8K test、MATH-500 与三项通用回归任务不发送给 Teacher，不进入训练、超参数选择或 checkpoint 筛选。

答案抽取与数学等价判断使用 [Math-Verify](https://github.com/huggingface/Math-Verify)，完整模型评估使用 [LightEval](https://github.com/huggingface/lighteval) 的现成任务。主计划已经生成 Qwen3.5-9B Base 与普通 SFT 的回归结果；本专题只对 D4 的唯一胜出 SFT 运行相同集合，未入选的中间 adapter 不重复评估。专题不编写 benchmark、gold labels 或另一套评估结果系统。

### 2.3　模型分工

| 角色 | 模型 | 用途 |
| --- | --- | --- |
| 首选 Teacher | `openai/gpt-oss-20b` | 生成 reasoning、final answer 与压缩版推理 |
| 备选 Teacher | `Qwen/Qwen3.8-27B` | D1 前可替代首选 Teacher；选定后固定为唯一 Teacher |
| 开发 Student | `Qwen/Qwen3.5-2B-Base` | 使用小数据和短训练检查数据、模板与 loss |
| 主实验 Student | `Qwen/Qwen3.5-9B-Base` | 完成三类 SFT 蒸馏 |

2B 只用于排除流程错误，不形成研究结论。9B 承担完整学习闭环。

### 2.4　固定工具链

| 环节 | 固定工具 | 使用边界 |
| --- | --- | --- |
| 数据加载与转换 | Hugging Face Datasets | `select`、`map`、`filter`、fingerprint 与 Parquet |
| Teacher 批量生成 | [Open R1](https://github.com/huggingface/open-r1) `generate.py` / [Distilabel](https://github.com/argilla-io/distilabel) | 使用现成 pipeline、cache 和 vLLM backend |
| 中断恢复 | [Distilabel pipeline cache](https://distilabel.argilla.io/latest/sections/how_to_guides/advanced/caching/) | 复用步骤中间产物，不自建分片恢复系统 |
| gpt-oss 格式 | [openai-harmony](https://github.com/openai/harmony) 或推理框架结构化返回 | 不手写 Harmony tokenizer 或 channel parser |
| 答案验证 | Math-Verify | 只增加字段适配，不重写数学判定逻辑 |
| SFT | TRL + PEFT + Accelerate | 使用官方 trainer、collator、YAML 和 checkpoint 能力 |
| 正式评估 | LightEval | 使用 `--save-details` 保存逐样本结果 |
| 训练记录 | TensorBoard | 保存曲线与核心指标，不搭建实验平台 |

Teacher 数据生成基线固定为 Open R1 `1416fa0`、Distilabel `313fac8` 与 openai-harmony `abd677f`。TRL 和 LightEval 的 revision 在首次试运行时冻结。若兼容性问题需要修改版本，只更新环境锁定文件和运行清单。

### 2.5　三类序列级监督

每个 prompt 的多个 Teacher 候选完整保留在原始候选池中。构造 SFT 数据时，按固定 candidate ID 顺序从通过验证的候选中选择一个规范候选，并从同一个候选派生：

| 监督形式 | 构造方式 | 学习问题 |
| --- | --- | --- |
| Answer-only | 只保留最终答案 | 最短监督能否学习任务映射 |
| Compressed rationale | 使用冻结的 Teacher 压缩提示词生成短步骤，再次运行 Math-Verify | 短推理能否平衡正确率与成本 |
| Full reasoning | 使用解析后的完整 reasoning 与 final answer | 完整推理是否提高能力并增加长度成本 |

Compressed rationale 只对每个 prompt 的规范候选执行一次，不采用人工摘要或逐条规则改写。压缩结果未通过 verifier 时不进入 Compressed rationale Dataset；其余原始候选保留在候选池中，不重复压缩。

### 2.6　最小代码边界

专题只维护三个薄适配函数：

```text
GSM8K row → Teacher generation input
Teacher structured output → Math-Verify input
candidate + verifier result → TRL SFT row
```

其余能力由固定工具链负责。项目不增加通用任务调度器、分布式生成框架、checkpoint 管理器、数据标注界面或评估数据库。

---

## 三、时间安排

### 3.1　三周半总览

| 周次 | 核心工作 | 主要交付物 |
| ---: | --- | --- |
| 第 7 周 | 完成 D1、D2，冻结 Teacher pipeline 和三类训练数据 | 生成 YAML、验证记录、Teacher 候选与三类 Dataset |
| 第 8 周 | 完成 D3，启动 D4 的 9B 三类监督对照 | 2B 调试记录、冻结 YAML、9B 首轮结果 |
| 第 9 周 | 完成 D4 | 9B SFT 蒸馏结果与统一评估 |
| 第 10 周前半 | 整理分析和复现入口 | 分析 notebook、专题总结 |

### 3.2　投入预算

| 类型 | 核心总投入（含复习） |
| --- | ---: |
| 设计与必要学习 | 12～16 小时 |
| 工具配置与三个薄适配 | 15～20 小时 |
| 训练与评估操作 | 35～48 小时 |
| 分析与结项 | 14～20 小时 |
| **合计** | **76～104 小时** |

较低预算对应完成 9B 核心闭环后结项；较高预算包含一次胜出方案复现。任何新增实验都从机动时间中分配，不挤占核心方法的学习。

---

## 四、阶段执行计划

### 阶段 D1　工具链复用与 100 条最小闭环

> **计划窗口**　第 7 周前半
>
> **预计总投入**　8.5～12 小时，其中首次学习与执行 6.5～9 小时，第三天复习 2～3 小时
>
> **阶段目标**　确认现成工具可以连通 Teacher、verifier、TRL 数据格式和 Student forward。

#### D1.1　固定入口

从 Open R1 `generate.py` 或其 Distilabel 示例开始，只修改：

- Teacher model ID 与 revision；
- GSM8K prompt 字段映射；
- generation config；
- 输出字段映射；
- 本地或 Hugging Face Dataset 保存位置。

Teacher 推理由 vLLM backend 承担。Harmony 格式优先读取推理框架返回的结构化字段；需要直接解析 token 时调用 `openai-harmony`。

#### D1.2　最小闭环

直接读取 `gsm8k_split_v1.json` 的 `smoke_100`，固定使用与 MiniMind Sequence-level Distillation 相同的 100 条 train prompts，执行：

```text
Datasets select
→ Distilabel / vLLM 多候选生成
→ Harmony reasoning / final 读取
→ Math-Verify 自动判定
→ TRL prompt-completion 格式
→ 2B 单批 forward
```

只读抽查 10 条通过结果和 10 条拒绝结果，用于确认字段、解析和 verifier 行为。抽查不修改答案或标签。

#### D1.3　阶段交付物

- 一份 Teacher generation YAML
- 一份包含 100 条 prompts 的缓存运行结果
- 三个薄适配函数
- Math-Verify 通过、失败与无法解析的自动测试
- 一批可被 TRL 和 2B Student 读取的数据

#### D1.4　验收标准

- 中断后能够通过 Distilabel cache 继续；
- Harmony reasoning 与 final answer 能够稳定读取；
- 任意结果能够定位到 prompt、Teacher revision 和 generation config；
- 训练数据无需逐条修改即可执行 Student forward。

---

### 阶段 D2　正式 Teacher 数据生成

> **计划窗口**　第 7 周中后段
>
> **预计总投入**　12～17 小时，其中首次学习与执行 9～13 小时，第三天复习 3～4 小时
>
> **阶段目标**　使用冻结 pipeline 自动产生三类序列监督所需的候选池。

#### D2.1　多候选生成

对冻结的 6,973 条 GSM8K train prompts 生成固定数量候选。第一版建议每题 4 个候选；该数值在 100 条试运行后冻结。

每个候选只保留必要字段：

```text
sample_id
prompt
candidate_id
raw_generation
reasoning
final_answer
verifier_pass
rejection_reason
teacher_model_revision
generation_config_id
```

生成、缓存、批次恢复与中间产物由 Distilabel 管理。数据转换使用 Datasets `map/filter`，版本由 Dataset fingerprint、Teacher revision 和 Git commit 共同标识。

#### D2.2　自动验证

固定执行以下自动检查并分别保存结果：

1. 输出与 channel 可以解析；
2. Math-Verify 可以抽取最终答案；
3. 最终答案与 GSM8K gold answer 等价；
4. 输出未截断且长度处于上限内；
5. 同一 prompt 内移除完全重复候选。

未通过全部检查的候选保留在原始结果中并标记拒绝原因，不进入 SFT 数据。自动汇总解析率、验证通过率、拒绝原因和长度分布。

#### D2.3　三类训练数据

对每个 prompt，按固定 candidate ID 顺序从通过验证的候选中选择一个规范候选：

- Answer-only：从规范候选提取 final answer；
- Full reasoning：保留同一规范候选的 reasoning 与 final answer；
- Compressed rationale：只对该规范候选运行一次冻结的 Teacher 压缩步骤，再次通过 Math-Verify。

其余候选继续保留在原始候选池中。正式 D4 对照固定使用三类 SFT 数据的共同 prompt 交集，确保只改变监督形式。因自动验证产生的交集外样本只计入数据统计，不通过人工补齐。

#### D2.4　阶段交付物

- 一份带 fingerprint 的原始 Teacher candidate Dataset
- 三类 TRL prompt-completion Dataset
- 自动生成的数据统计摘要
- Teacher generation config 与压缩 config

#### D2.5　验收标准

- 所有训练样本均可回溯至 Teacher 原始结果；
- 生成中断无需手写恢复逻辑；
- 三类数据不包含 Harmony 控制标记；
- 数据准备全程没有人工改写、补标签或逐条修复。

---

### 阶段 D3　2B 短流程调试

> **计划窗口**　第 8 周前半
>
> **预计总投入**　15.5～19.5 小时，其中首次学习与执行 12～15 小时，第三天复习 3.5～4.5 小时
>
> **阶段目标**　用低成本 Student 排除数据、模板、长度和训练配置错误。

#### D3.1　执行范围

在 `Qwen/Qwen3.5-2B-Base` 上分别使用三类监督完成短训练。固定读取 `smoke_100` 中同时存在 Answer-only、Compressed rationale 与 Full reasoning 的 prompt 交集，并按原始 GSM8K row ID 排序。三组复用同一份索引、token 预算和固定 seed，只保存最终 adapter。

Base baseline、训练配置和评估入口直接继承主计划。2B 只补做同 revision 的 Base evaluation，不再训练普通 SFT baseline；普通公开数据 SFT 对照统一复用主计划的 9B 正式结果。

#### D3.2　检查内容

- TRL chat template 与 completion mask；
- 有效 label token 数量；
- 截断、重复、空响应与异常长输出；
- adapter 保存、加载与批量生成；
- Math-Verify 与 LightEval 结果读取；
- TensorBoard loss 与吞吐记录。

#### D3.3　阶段交付物

- 固定 prompt 索引与三组 2B 短训练记录
- 一份数据、模板和长度问题清单
- 进入 9B 实验的冻结 YAML

#### D3.4　验收标准

- 三类数据均能稳定训练和评估；
- 主要格式问题已由程序定位；
- 9B 阶段无需修改数据 schema、chat template 或评估入口。

---

### 阶段 D4　9B 序列蒸馏主实验

> **计划窗口**　第 8 周后半～第 9 周前半
>
> **预计总投入**　27～32.5 小时，其中首次学习与执行 21～25 小时，第三天复习 6～7.5 小时
>
> **阶段目标**　在统一预算下比较三类 Teacher 监督，形成专题的主要结论。

#### D4.1　实验组

主计划已有的以下结果直接进入对照表：

1. Qwen3.5-9B Base；
2. GSM8K 普通公开数据 QLoRA SFT。

本阶段新增：

3. Answer-only distillation；
4. Compressed-rationale distillation；
5. Full-reasoning distillation。

三个新增实验从同一 Base revision 初始化，使用同一固定 seed 和 Student 训练 token 总量。核心路线不运行第二个随机种子，也不增加固定 prompt 数量的第二套完整训练矩阵。

#### D4.2　比较指标

- GSM8K development accuracy，用于选择胜出方案；
- 冻结方案后统一运行的 GSM8K test 与 MATH-500 accuracy；
- 胜出方案冻结后运行的 IFEval、MMLU 与 TruthfulQA 回归指标；
- 最终答案可解析率；
- 平均输出 token 与 reasoning token；
- 截断、重复和异常长输出比例；
- 推理延迟与批量吞吐。

#### D4.3　阶段交付物

- 三个 9B 蒸馏 adapter 与冻结 YAML
- 一张 Base、普通 SFT 与三类蒸馏的核心对照表
- LightEval 结果和逐样本 details
- 胜出方案相对 Base 与普通 SFT 的通用回归对照
- 一段监督形式阶段结论

#### D4.4　验收标准

- 主实验只改变 Teacher 监督形式；
- 每个结果均能定位到数据 fingerprint、模型 revision 和训练 YAML；
- 已根据 development 正确率、长度与推理成本选出一个胜出方案，test 与 MATH-500 未参与选择；
- 通用回归集合只在胜出方案冻结后运行，未参与方案选择；
- 分析过程直接读取 LightEval details，不维护第二套逐样本结果文件。

---

### 阶段 D5　RL 衔接（延期）

本阶段延期到 `study/rl-system`，不参与 Distillation 专题的时间预算与验收。Teacher 候选与
Math-Verify 结果继续完整保存，未来可由 RL 专题只读复用。

---

## 五、实验与评估规范

### 5.1　核心实验矩阵

| 实验 | 2B | 9B |
| --- | :---: | :---: |
| Base | 读取/检查 | 继承主计划 |
| 普通 GSM8K SFT | 读取/检查 | 继承主计划 |
| Answer-only | 短流程 | 必做 |
| Compressed rationale | 短流程 | 必做 |
| Full reasoning | 短流程 | 必做 |

### 5.2　必须固定的条件

比较三类监督形式时固定：

- prompt 集合与 split；
- Base model revision；
- Teacher generation protocol；
- Student tokenizer 与 chat template；
- 数据转换代码与 Math-Verify revision；
- Student 总训练 token；
- optimizer、learning rate、scheduler 与 effective batch size；
- evaluation task 与 generation config；
- IFEval、MMLU 与 TruthfulQA 的 LightEval revision、task ID 和 metric；
- 核心固定 seed。

### 5.3　最小实验记录

每次训练只需保存 YAML 和以下运行清单：

```text
git_commit
model_id / revision
dataset_fingerprint
teacher_generation_config_id
training_config
seed
train_tokens / effective_label_tokens
trainable_parameters
peak_vram / tokens_per_second
adapter_or_checkpoint
lighteval_result_dir
```

训练指标写入 TensorBoard，逐样本预测与指标写入 LightEval details。相同信息不在第三处手工抄写。

### 5.4　停止规则

出现以下情况时停止扩大实验：

- 2B 仍存在 schema、mask、截断或评估错误；
- 某类监督在 9B 上出现明确格式崩溃或能力退化；
- 9B 结果没有形成可信的胜出方案。

停止实验属于有效结论，不通过增加模型、seed 或人工修正数据掩盖问题。

---

## 六、项目里程碑

### 里程碑一　Teacher 数据闭环

> **目标时间**　第 7 周末

- Open R1 / Distilabel 生成可以中断恢复；
- Harmony 与 Math-Verify 流程已通过 100 条测试；
- 正式 Teacher 候选生成已经启动或完成。

### 里程碑二　序列蒸馏主结论

> **目标时间**　第 9 周前半

- 2B 短流程无结构性错误；
- 9B 三类监督形式已经完成统一评估；
- 胜出方案已经完成固定通用回归评估；
- 已选出一个胜出方案。

### 里程碑三　验证与结项

> **目标时间**　第 10 周前半

- 分析 notebook、核心结果表和专题总结已经完成；
- 数据、配置、adapter 和评估结果可以相互追溯。

---

## 七、条件式扩展

核心路线完成后，每次只选择一个扩展。

### 7.1　稳健性与更大规模

从以下项目中选择一个：

- 对 9B 胜出方案增加一个随机种子；
- 在资源充分时为 9B 胜出方案做一次独立复现。

### 7.2　在线蒸馏

让 Teacher 只针对 Student 的自动验证失败样本继续生成监督。该方向涉及异步生成和数据新鲜度，只在离线流程稳定后启动。

### 7.3　第二 Teacher

增加一个同模型家族 Teacher，比较跨 tokenizer 与同家族 Teacher 数据。第二 Teacher 不影响核心专题的完成判定。

---

## 八、结项标准

完成核心专题时，应能够独立说明并演示：

- sequence-level distillation 与 logit distillation 的适用边界；
- Teacher 多候选如何由现成工具生成、缓存和恢复；
- Harmony reasoning 与 final answer 如何进入自动验证；
- 三类序列监督如何从同一候选池派生；
- TRL SFTTrainer 接收什么数据；
- 如何使用统一预算比较正确率、输出长度和推理成本；
- 如何用固定回归集合识别数学专项训练造成的通用能力变化；
- 如何依据固定评估与资源预算决定是否进行一次独立复现。

项目最终只保留以下核心产物：

```text
一份实验卡
一份 Teacher generation YAML
一份带 fingerprint 的 Teacher candidate Dataset
三类 SFT Dataset
9B 三类蒸馏 adapter
TensorBoard 日志与 LightEval 结果目录
一个分析 notebook
一份专题总结
```

当这些产物能够相互追溯、核心实验可以从 YAML 重新启动、主要学习问题得到证据支持的回答时，本专题结项。
