# 模型蒸馏专题学习计划

> *复用主计划已经建立的 Qwen 训练资产，用一条轻量、可复现的流水线完成序列级蒸馏与偏好蒸馏。*

---

## 目录

1. [专题定位](#一专题定位)
2. [研究问题与技术路线](#二研究问题与技术路线)
3. [时间安排](#三时间安排)
4. [阶段执行计划](#四阶段执行计划)
5. [实验与评估规范](#五实验与评估规范)
6. [项目里程碑](#六项目里程碑)
7. [条件式扩展](#七条件式扩展)
8. [结项标准](#八结项标准)

---

## 一、专题定位

### 1.1　计划目标

本计划承接 [LLM 自学项目计划](./LLM自学计划.md) 的阶段 10。在 Qwen Base、4B QLoRA SFT、TRL 数据流和 LightEval 评估已经跑通后，集中学习以下内容：

- Teacher 多候选数据的批量生成与自动验证；
- Answer-only、Compressed rationale 与 Full reasoning 三类序列级监督；
- 从可验证候选中自动构造 preference pairs；
- 使用 TRL 完成正式的 SFT 蒸馏与 DPO 蒸馏；
- 在统一口径下比较正确率、输出长度和推理成本；
- 将 4B 主实验的胜出方案按条件迁移至 8B。

本专题以熟悉算法、框架和研究流程为目标。核心路线保留每种蒸馏信号的第一次完整实践，减少多模型重复训练、数据平台开发和论文级稳健性验证。

正式数据直接采用公开 benchmark、官方答案和现成评估工具。学习者不编写题目、不制作标签、不逐条清洗、不人工判断 preference。无法解析、输出截断或重复的候选不进入派生数据；可解析但答案错误的候选保留在原始候选池中，可按确定性规则用于 DPO rejected。

### 1.2　直接继承的项目资产

进入专题前，以下产物应已由主计划生成并冻结，本专题直接读取，不重新制作：

| 资产 | 继承内容 |
| --- | --- |
| 数据基线 | 阶段 8 生成、阶段 10 核验的 `gsm8k_split_v1.json`，包含固定 train/development、`smoke_100`、样本 ID 与 revision |
| 泛化评估 | MATH-500 固定 revision，只读 |
| 通用回归 | IFEval、MMLU 与 TruthfulQA 的冻结 LightEval 任务配置和 Base / SFT 结果 |
| 模型基线 | Qwen3-4B Base 与普通 GSM8K QLoRA SFT 结果 |
| 训练规范 | TRL YAML、Accelerate 启动配置、TensorBoard 记录方式 |
| 评估规范 | Math-Verify、LightEval task、generation config 与结果目录 |
| 版本规范 | Git commit、模型 revision、Dataset fingerprint 与 checkpoint 索引 |

主计划中的公开 UltraFeedback DPO 仅用于学习 TRL 数据流，不作为本专题的数学偏好 baseline，也不进入专题结论。

### 1.3　前置基础

进入本计划前，应能够：

- 解释交叉熵、KL 散度、SFT、DPO 与 sequence-level distillation；
- 使用 Transformers、Datasets、PEFT、TRL 和 Accelerate；
- 加载 Qwen Base、adapter 和合并后的 checkpoint；
- 使用 vLLM 或 SGLang 批量生成；
- 使用 Math-Verify 与 LightEval 完成独立评估；
- 读取已有 YAML、日志、Dataset fingerprint 和 checkpoint 记录。

MiniMind 阶段已经完成同 tokenizer 的 logit KD。本专题不重复 logit distillation，也不重新实现训练循环、DPO loss 或分布式训练器。

### 1.4　时间与资源

| 项目 | 安排 |
| --- | --- |
| 每周投入 | 35 小时 |
| 每周新内容 / 第三天复习 | 约 27 小时 / 约 8 小时 |
| 核心周期 | 3～3.5 周 |
| 计划窗口 | 第 7 周～第 10 周前半 |
| 核心投入 | 约 91～123 小时 |
| 计算资源 | 租用多卡高算力服务器 |
| Teacher | `openai/gpt-oss-20b` |
| 开发 Student | `Qwen/Qwen3-1.7B-Base` |
| 主实验 Student | `Qwen/Qwen3-4B-Base` |
| 条件式规模验证 | `Qwen/Qwen3-8B-Base` |
| 模型与数据平台 | Hugging Face |

每周投入只计算需要持续参与的学习、配置、检查、分析和记录时间。批量生成与训练的无人值守等待时间单独记录。每天完成新内容后，在第三天（学习日记为 D 时的 D+2）安排约为首次学习时间 30% 的复习；复习时间已经计入阶段总投入和每周 35 小时。复习以脱离配置说明数据流、复查一项 verifier 或 mask 结果、重新运行一个关键入口为主。

阶段运行完成后可以继续启动后续流水线，对应第三天复习完成后才关闭阶段里程碑。阶段 D4 完成后使用实际主动投入校准 D5、D6；偏离预算超过 20% 时才调整尚未启动的工作，不扩大正式实验矩阵。

8B 只复现胜出方案，14B、多随机种子、第二类偏好 judge 和多 Teacher 均属于条件式扩展。新增算力优先缩短迭代等待，不用于无条件扩大实验矩阵。

本专题完成后，可根据学习目标先执行主计划的可选 GRPO；随后进入阶段 12 的架构研究。未执行 GRPO 时直接进入阶段 12。

### 1.5　专题主线

```text
继承主计划 baseline
→ Open R1 / Distilabel 生成 Teacher 候选
→ Harmony 解析与 Math-Verify 自动验证
→ 派生三类序列监督
→ 1.7B 短流程调试
→ 4B 正式监督形式对照
→ 自动构造 verifier preference pairs
→ 4B DPO 蒸馏
→ 条件式 8B 验证
→ 一份分析 notebook 与专题总结
```

---

## 二、研究问题与技术路线

### 2.1　核心研究问题

专题只回答一个核心问题：

> 在固定 Teacher、公开任务与 Student 训练预算下，Answer-only、Compressed rationale、Full reasoning 和 verifier preference signal 对 Qwen3-4B 的正确率、输出长度与推理成本产生怎样的影响？

核心变量只有两个：

1. Teacher 提供的序列监督形式；
2. 最佳序列蒸馏模型是否继续接受 verifier-filtered DPO。

8B 只用于检查胜出方案能否迁移至更大规模，不承担第二轮完整消融。

### 2.2　固定数据与评测基线

| 用途 | 数据集 | 固定用法 |
| --- | --- | --- |
| Teacher prompts 与正式训练 | [`openai/gsm8k`](https://huggingface.co/datasets/openai/gsm8k)，config `main` | `gsm8k_split_v1.json` 中冻结的 6,973 条 train prompts |
| Development | GSM8K train 子集 | 同一清单中的 500 条固定 row ID |
| 主评估 | GSM8K 官方 test | 1,319 条，只读 |
| 泛化评估 | [`HuggingFaceH4/MATH-500`](https://huggingface.co/datasets/HuggingFaceH4/MATH-500) | 500 条，只读 |
| 指令遵循回归 | IFEval | 继承主计划冻结的 LightEval task，只读 |
| 通用能力回归 | MMLU | 继承主计划冻结的 LightEval task，只读 |
| 真实性风险信号 | TruthfulQA multiple-choice | 继承主计划冻结的 LightEval task，只读 |

GSM8K test、MATH-500 与三项通用回归任务不发送给 Teacher，不进入训练、preference pair 构造、超参数选择或 checkpoint 筛选。

答案抽取与数学等价判断使用 [Math-Verify](https://github.com/huggingface/Math-Verify)，完整模型评估使用 [LightEval](https://github.com/huggingface/lighteval) 的现成任务。主计划已经生成 Qwen3-4B Base 与普通 SFT 的回归结果；本专题只对 D4 的唯一胜出 SFT、D5 的最终 DPO 和条件式 8B 关键 checkpoint 运行相同集合，未入选的中间 adapter 不重复评估。专题不编写 benchmark、gold labels 或另一套评估结果系统。

### 2.3　模型分工

| 角色 | 模型 | 用途 |
| --- | --- | --- |
| Teacher | `openai/gpt-oss-20b` | 生成 reasoning、final answer 与压缩版推理 |
| 开发 Student | `Qwen/Qwen3-1.7B-Base` | 使用小数据和短训练检查数据、模板与 loss |
| 主实验 Student | `Qwen/Qwen3-4B-Base` | 完成三类 SFT 蒸馏和一次 DPO 蒸馏 |
| 规模验证 Student | `Qwen/Qwen3-8B-Base` | 仅在启动条件满足时复现普通 SFT 与胜出方案 |

1.7B 只用于排除流程错误，不形成研究结论。4B 承担完整学习闭环。8B 提供一次规模迁移体验。

### 2.4　固定工具链

| 环节 | 固定工具 | 使用边界 |
| --- | --- | --- |
| 数据加载与转换 | Hugging Face Datasets | `select`、`map`、`filter`、fingerprint 与 Parquet |
| Teacher 批量生成 | [Open R1](https://github.com/huggingface/open-r1) `generate.py` / [Distilabel](https://github.com/argilla-io/distilabel) | 使用现成 pipeline、cache 和 vLLM backend |
| 中断恢复 | [Distilabel pipeline cache](https://distilabel.argilla.io/latest/sections/how_to_guides/advanced/caching/) | 复用步骤中间产物，不自建分片恢复系统 |
| gpt-oss 格式 | [openai-harmony](https://github.com/openai/harmony) 或推理框架结构化返回 | 不手写 Harmony tokenizer 或 channel parser |
| 答案验证 | Math-Verify | 只增加字段适配，不重写数学判定逻辑 |
| SFT / DPO | TRL + PEFT + Accelerate | 使用官方 trainer、collator、YAML 和 checkpoint 能力 |
| 正式评估 | LightEval | 使用 `--save-details` 保存逐样本结果 |
| 训练记录 | TensorBoard | 保存曲线与核心指标，不搭建实验平台 |

Teacher 数据生成基线固定为 Open R1 `1416fa0`、Distilabel `313fac8` 与 openai-harmony `abd677f`。TRL 和 LightEval 的 revision 在首次试运行时冻结。若兼容性问题需要修改版本，只更新环境锁定文件和运行清单。

### 2.5　三类序列级监督

每个 prompt 的多个 Teacher 候选完整保留在原始候选池中。构造 SFT 数据时，按固定 candidate ID 顺序从通过验证的候选中选择一个规范候选，并从同一个候选派生：

| 监督形式 | 构造方式 | 学习问题 |
| --- | --- | --- |
| Answer-only | 只保留最终答案 | 最短监督能否学习任务映射 |
| Compressed rationale | 使用冻结的 Teacher 压缩提示词生成短步骤，再次运行 Math-Verify | 短推理能否平衡正确率与成本 |
| Full reasoning | 使用解析后的完整 reasoning 与 final answer | 完整推理是否提高能力并增加长度成本 |

Compressed rationale 只对每个 prompt 的规范候选执行一次，不采用人工摘要或逐条规则改写。压缩结果未通过 verifier 时不进入 Compressed rationale Dataset；其余原始候选保留给后续 preference pair 构造，不重复压缩。

### 2.6　最小代码边界

专题只维护三个薄适配函数：

```text
GSM8K row → Teacher generation input
Teacher structured output → Math-Verify input
candidate + verifier result → TRL SFT / DPO row
```

其余能力由固定工具链负责。项目不增加通用任务调度器、分布式生成框架、checkpoint 管理器、数据标注界面或评估数据库。

---

## 三、时间安排

### 3.1　三周半总览

| 周次 | 核心工作 | 主要交付物 |
| ---: | --- | --- |
| 第 7 周 | 完成 D1、D2，冻结 Teacher pipeline 和三类训练数据 | 生成 YAML、验证记录、Teacher 候选与三类 Dataset |
| 第 8 周 | 完成 D3，启动 D4 的 4B 三类监督对照 | 1.7B 调试记录、冻结 YAML、4B 首轮结果 |
| 第 9 周 | 完成 D4，自动构造 preference pairs 并执行 D5 | 4B SFT 蒸馏结果、DPO adapter、统一评估 |
| 第 10 周前半 | 按条件执行 D6 的 8B 验证，整理分析和复现入口 | 规模验证结果或停止记录、分析 notebook、专题总结 |

### 3.2　投入预算

| 类型 | 核心总投入（含复习） |
| --- | ---: |
| 设计与必要学习 | 12～16 小时 |
| 工具配置与三个薄适配 | 15～20 小时 |
| 训练与评估操作 | 48～65 小时 |
| 分析与结项 | 16～22 小时 |
| **合计** | **91～123 小时** |

较低预算对应完成 4B 核心闭环后结项；较高预算包含条件式 8B 验证和一次胜出方案复现。任何新增实验都从机动时间中分配，不挤占核心方法的学习。

---

## 四、阶段执行计划

### 阶段 D1　工具链复用与 100 条最小闭环

> **计划窗口**　第 7 周前半
>
> **预计总投入**　8.5～12 小时，其中首次学习与执行 6.5～9 小时，第三天复习 2～3 小时
>
> **阶段目标**　确认现成工具可以连通 Teacher、verifier、TRL 数据格式和 Student forward。

#### D1.1　固定入口

从 Open R1 `generate.py` 或其 Distilabel 示例开始，只修改：

- Teacher model ID 与 revision；
- GSM8K prompt 字段映射；
- generation config；
- 输出字段映射；
- 本地或 Hugging Face Dataset 保存位置。

Teacher 推理由 vLLM backend 承担。Harmony 格式优先读取推理框架返回的结构化字段；需要直接解析 token 时调用 `openai-harmony`。

#### D1.2　最小闭环

直接读取 `gsm8k_split_v1.json` 的 `smoke_100`，固定使用与 MiniMind Sequence-level Distillation 相同的 100 条 train prompts，执行：

```text
Datasets select
→ Distilabel / vLLM 多候选生成
→ Harmony reasoning / final 读取
→ Math-Verify 自动判定
→ TRL prompt-completion 格式
→ 1.7B 单批 forward
```

只读抽查 10 条通过结果和 10 条拒绝结果，用于确认字段、解析和 verifier 行为。抽查不修改答案或标签。

#### D1.3　阶段交付物

- 一份 Teacher generation YAML
- 一份包含 100 条 prompts 的缓存运行结果
- 三个薄适配函数
- Math-Verify 通过、失败与无法解析的自动测试
- 一批可被 TRL 和 1.7B Student 读取的数据

#### D1.4　验收标准

- 中断后能够通过 Distilabel cache 继续；
- Harmony reasoning 与 final answer 能够稳定读取；
- 任意结果能够定位到 prompt、Teacher revision 和 generation config；
- 训练数据无需逐条修改即可执行 Student forward。

---

### 阶段 D2　正式 Teacher 数据生成

> **计划窗口**　第 7 周中后段
>
> **预计总投入**　12～17 小时，其中首次学习与执行 9～13 小时，第三天复习 3～4 小时
>
> **阶段目标**　使用冻结 pipeline 自动产生三类序列监督和后续偏好蒸馏所需的候选池。

#### D2.1　多候选生成

对冻结的 6,973 条 GSM8K train prompts 生成固定数量候选。第一版建议每题 4 个候选；该数值在 100 条试运行后冻结。

每个候选只保留必要字段：

```text
sample_id
prompt
candidate_id
raw_generation
reasoning
final_answer
verifier_pass
rejection_reason
teacher_model_revision
generation_config_id
```

生成、缓存、批次恢复与中间产物由 Distilabel 管理。数据转换使用 Datasets `map/filter`，版本由 Dataset fingerprint、Teacher revision 和 Git commit 共同标识。

#### D2.2　自动验证

固定执行以下自动检查并分别保存结果：

1. 输出与 channel 可以解析；
2. Math-Verify 可以抽取最终答案；
3. 最终答案与 GSM8K gold answer 等价；
4. 输出未截断且长度处于上限内；
5. 同一 prompt 内移除完全重复候选。

未通过全部检查的候选保留在原始结果中并标记拒绝原因，不进入 SFT 数据。自动汇总解析率、验证通过率、拒绝原因和长度分布。只有可解析、未截断、长度合规且未重复的错误候选可以进入阶段 D5 的 rejected 候选池。

#### D2.3　三类训练数据

对每个 prompt，按固定 candidate ID 顺序从通过验证的候选中选择一个规范候选：

- Answer-only：从规范候选提取 final answer；
- Full reasoning：保留同一规范候选的 reasoning 与 final answer；
- Compressed rationale：只对该规范候选运行一次冻结的 Teacher 压缩步骤，再次通过 Math-Verify。

其余候选继续保留在原始候选池中，只供阶段 D5 构造 preference pairs。正式 D4 对照固定使用三类 SFT 数据的共同 prompt 交集，确保只改变监督形式。因自动验证产生的交集外样本只计入数据统计，不通过人工补齐。

#### D2.4　阶段交付物

- 一份带 fingerprint 的原始 Teacher candidate Dataset
- 三类 TRL prompt-completion Dataset
- 自动生成的数据统计摘要
- Teacher generation config 与压缩 config

#### D2.5　验收标准

- 所有训练样本均可回溯至 Teacher 原始结果；
- 生成中断无需手写恢复逻辑；
- 三类数据不包含 Harmony 控制标记；
- 数据准备全程没有人工改写、补标签或逐条修复。

---

### 阶段 D3　1.7B 短流程调试

> **计划窗口**　第 8 周前半
>
> **预计总投入**　15.5～19.5 小时，其中首次学习与执行 12～15 小时，第三天复习 3.5～4.5 小时
>
> **阶段目标**　用低成本 Student 排除数据、模板、长度和训练配置错误。

#### D3.1　执行范围

在 `Qwen/Qwen3-1.7B-Base` 上分别使用三类监督完成短训练。固定读取 `smoke_100` 中同时存在 Answer-only、Compressed rationale 与 Full reasoning 的 prompt 交集，并按原始 GSM8K row ID 排序。三组复用同一份索引、token 预算和固定 seed，只保存最终 adapter。

Base baseline、训练配置和评估入口直接继承主计划。1.7B 只补做同 revision 的 Base evaluation，不再训练普通 SFT baseline；普通公开数据 SFT 对照统一复用主计划的 4B 正式结果。

#### D3.2　检查内容

- TRL chat template 与 completion mask；
- 有效 label token 数量；
- 截断、重复、空响应与异常长输出；
- adapter 保存、加载与批量生成；
- Math-Verify 与 LightEval 结果读取；
- TensorBoard loss 与吞吐记录。

#### D3.3　阶段交付物

- 固定 prompt 索引与三组 1.7B 短训练记录
- 一份数据、模板和长度问题清单
- 进入 4B 实验的冻结 YAML

#### D3.4　验收标准

- 三类数据均能稳定训练和评估；
- 主要格式问题已由程序定位；
- 4B 阶段无需修改数据 schema、chat template 或评估入口。

---

### 阶段 D4　4B 序列蒸馏主实验

> **计划窗口**　第 8 周后半～第 9 周前半
>
> **预计总投入**　27～32.5 小时，其中首次学习与执行 21～25 小时，第三天复习 6～7.5 小时
>
> **阶段目标**　在统一预算下比较三类 Teacher 监督，形成专题的主要结论。

#### D4.1　实验组

主计划已有的以下结果直接进入对照表：

1. Qwen3-4B Base；
2. GSM8K 普通公开数据 QLoRA SFT。

本阶段新增：

3. Answer-only distillation；
4. Compressed-rationale distillation；
5. Full-reasoning distillation。

三个新增实验从同一 Base revision 初始化，使用同一固定 seed 和 Student 训练 token 总量。核心路线不运行第二个随机种子，也不增加固定 prompt 数量的第二套完整训练矩阵。

#### D4.2　比较指标

- GSM8K development accuracy，用于选择胜出方案；
- 冻结方案后统一运行的 GSM8K test 与 MATH-500 accuracy；
- 胜出方案冻结后运行的 IFEval、MMLU 与 TruthfulQA 回归指标；
- 最终答案可解析率；
- 平均输出 token 与 reasoning token；
- 截断、重复和异常长输出比例；
- 推理延迟与批量吞吐。

#### D4.3　阶段交付物

- 三个 4B 蒸馏 adapter 与冻结 YAML
- 一张 Base、普通 SFT 与三类蒸馏的核心对照表
- LightEval 结果和逐样本 details
- 胜出方案相对 Base 与普通 SFT 的通用回归对照
- 一段监督形式阶段结论

#### D4.4　验收标准

- 主实验只改变 Teacher 监督形式；
- 每个结果均能定位到数据 fingerprint、模型 revision 和训练 YAML；
- 已根据 development 正确率、长度与推理成本选出一个胜出方案，test 与 MATH-500 未参与选择；
- 通用回归集合只在胜出方案冻结后运行，未参与方案选择；
- 分析过程直接读取 LightEval details，不维护第二套逐样本结果文件。

---

### 阶段 D5　Verifier-filtered DPO 蒸馏

> **计划窗口**　第 9 周后半
>
> **预计总投入**　14.5～19.5 小时，其中首次学习与执行 11～15 小时，第三天复习 3.5～4.5 小时
>
> **阶段目标**　将同一批 Teacher 候选自动转换为客观 preference signal，完成一次正式数学 DPO 实验。

#### D5.1　自动偏好对

每个 preference pair 必须来自同一 prompt：

```text
chosen   = Math-Verify 通过且结构检查合格的候选
rejected = Math-Verify 未通过但结构检查合格的候选
```

存在多个候选时，chosen 按固定 candidate ID 顺序选择；rejected 优先选择与 chosen 长度最接近的错误候选，以减少长度成为偏好捷径。没有清晰正确/错误候选对的 prompt 直接跳过。

配对脚本只保存 verifier 结果、候选长度和确定性选择规则。不人工判断“推理质量”“可定位错误”或“表达更好”，也不手工填写配对理由。

#### D5.2　DPO 训练

使用阶段 D4 的胜出 SFT adapter 作为 policy 初始化，通过 TRL `DPOTrainer` 完成：

- policy / reference model 加载；
- preference Dataset 读取；
- DPO loss 与 preference accuracy 记录；
- adapter 保存和恢复；
- 直接读取阶段 D4 已冻结的胜出 SFT 评估，作为 DPO 前结果；
- 只对 DPO 后 checkpoint 新增独立 LightEval 与固定通用回归评估。

核心对照只有：

1. 普通公开数据 SFT；
2. 最佳序列蒸馏 SFT；
3. 最佳序列蒸馏 SFT + Verifier-filtered DPO。

#### D5.3　阶段交付物

- 一份自动生成的 verifier preference Dataset
- 一份 DPO YAML 与 adapter
- 复用 D4 基线形成的 DPO 前后正确率、格式和长度对照
- 复用 D4 基线形成的 DPO 前后通用回归对照
- preference 蒸馏的收益与风险记录

#### D5.4　验收标准

- chosen / rejected 标签全部由 Math-Verify 自动产生；
- DPO 训练没有引入人工 preference；
- 能够说明序列级监督和 preference signal 的差异；
- 正确率变化能够与长度偏置、格式变化区分。
- 数学能力变化能够与通用能力回退区分。

---

### 阶段 D6　条件式 8B 验证与结项

> **计划窗口**　第 10 周前半
>
> **预计总投入**　13～22 小时，其中首次学习与执行 10～17 小时，第三天复习 3～5 小时
>
> **阶段目标**　在学习收益明确时验证规模迁移，并用最少产物完成结项。

#### D6.1　启动条件

只有以下条件全部满足时执行 8B：

- 4B 三类序列监督已经形成明确排序；
- 胜出方案的数据与训练流程没有系统性错误；
- LightEval 指标、格式与输出长度共同支持继续验证；
- 剩余算力和学习时间足以完成同口径评估。

条件未满足时，记录停止理由并直接结项。

#### D6.2　验证范围

8B 最多运行：

1. Base evaluation；
2. 普通 GSM8K SFT baseline；
3. 4B 阶段胜出的序列蒸馏方案；
4. DPO 方案，仅在 4B 上产生稳定增益时执行。

本阶段不重复三类监督消融。核心路线只使用一个固定 seed；第二个 seed 和 14B 进入条件式扩展。

#### D6.3　结项产物

- 一份分析 notebook，直接读取 TensorBoard 与 LightEval 结果；
- 一张 4B 核心结果表和可选 8B 验证表；
- 一张关键 checkpoint 的通用回归表；
- 一份简洁专题总结，记录结论、失败、限制和后续选择；
- 数据 fingerprint、YAML、adapter、评估目录的索引。

#### D6.4　验收标准

- 已说明 4B 结论能否迁移至 8B，或给出停止验证的明确依据；
- 未为结项额外开发报告系统、结果数据库或 checkpoint 管理器；
- 所有关键结论均能定位到现成日志和评估详情。

---

## 五、实验与评估规范

### 5.1　核心实验矩阵

| 实验 | 1.7B | 4B | 8B |
| --- | :---: | :---: | :---: |
| Base | 读取/检查 | 继承主计划 | 条件式评估 |
| 普通 GSM8K SFT | 读取/检查 | 继承主计划 | 条件式训练 |
| Answer-only | 短流程 | 必做 | — |
| Compressed rationale | 短流程 | 必做 | 胜出时执行 |
| Full reasoning | 短流程 | 必做 | 胜出时执行 |
| 最佳蒸馏 + Verifier-filtered DPO | — | 必做 | 4B 稳定增益时执行 |

8B 只会执行一个胜出的序列监督方案。表中的两个“胜出时执行”最多有一个进入实际训练。

### 5.2　必须固定的条件

比较三类监督形式时固定：

- prompt 集合与 split；
- Base model revision；
- Teacher generation protocol；
- Student tokenizer 与 chat template；
- 数据转换代码与 Math-Verify revision；
- Student 总训练 token；
- optimizer、learning rate、scheduler 与 effective batch size；
- evaluation task 与 generation config；
- IFEval、MMLU 与 TruthfulQA 的 LightEval revision、task ID 和 metric；
- 核心固定 seed。

### 5.3　最小实验记录

每次训练只需保存 YAML 和以下运行清单：

```text
git_commit
model_id / revision
dataset_fingerprint
teacher_generation_config_id
training_config
seed
train_tokens / effective_label_tokens
trainable_parameters
peak_vram / tokens_per_second
adapter_or_checkpoint
lighteval_result_dir
```

训练指标写入 TensorBoard，逐样本预测与指标写入 LightEval details。相同信息不在第三处手工抄写。

### 5.4　停止规则

出现以下情况时停止扩大实验：

- 1.7B 仍存在 schema、mask、截断或评估错误；
- 某类监督在 4B 上出现明确格式崩溃或能力退化；
- verifier preference pairs 数量不足或长度偏置明显；
- 4B 结果没有形成可信的胜出方案；
- 8B 只会重复已有结论且不增加新的学习问题。

停止实验属于有效结论，不通过增加模型、seed 或人工修正数据掩盖问题。

---

## 六、项目里程碑

### 里程碑一　Teacher 数据闭环

> **目标时间**　第 7 周末

- Open R1 / Distilabel 生成可以中断恢复；
- Harmony 与 Math-Verify 流程已通过 100 条测试；
- 正式 Teacher 候选生成已经启动或完成。

### 里程碑二　序列蒸馏主结论

> **目标时间**　第 9 周前半

- 1.7B 短流程无结构性错误；
- 4B 三类监督形式已经完成统一评估；
- 胜出方案已经完成固定通用回归评估；
- 已选出一个胜出方案。

### 里程碑三　偏好蒸馏完成

> **目标时间**　第 9 周末

- verifier preference Dataset 已自动生成；
- 4B DPO 已完成；
- 正确率、格式和长度变化已经比较。
- 已复用 D4 胜出 SFT 结果，与 DPO 后新增评估比较通用回归变化。

### 里程碑四　验证与结项

> **目标时间**　第 10 周前半

- 8B 已按条件执行或记录停止理由；
- 分析 notebook、核心结果表和专题总结已经完成；
- 数据、配置、adapter 和评估结果可以相互追溯。

---

## 七、条件式扩展

核心路线完成后，每次只选择一个扩展。

### 7.1　Teacher-ranked DPO

使用固定自动 judge 对多个正确候选评分，再构造第二类 preference Dataset。可采用 Distilabel 的现成 preference task，保存原始评分与自动理由。该扩展不引入人工排序。

### 7.2　稳健性与更大规模

从以下项目中选择一个：

- 对 4B 胜出方案增加一个随机种子；
- 对 8B 胜出方案增加一个随机种子；
- 在资源充分时用 14B 复现普通 SFT 与唯一胜出方案。

### 7.3　奖励蒸馏

利用已有 Teacher 候选和自动分数训练轻量 Reward Model，观察候选排序、rejection sampling 与 verifier 一致性。

### 7.4　在线蒸馏

让 Teacher 只针对 Student 的自动验证失败样本继续生成监督。该方向涉及异步生成和数据新鲜度，只在离线流程稳定后启动。

### 7.5　第二 Teacher

增加一个同模型家族 Teacher，比较跨 tokenizer 与同家族 Teacher 数据。第二 Teacher 不影响核心专题的完成判定。

---

## 八、结项标准

完成核心专题时，应能够独立说明并演示：

- sequence-level distillation 与 logit distillation 的适用边界；
- Teacher 多候选如何由现成工具生成、缓存和恢复；
- Harmony reasoning 与 final answer 如何进入自动验证；
- 三类序列监督如何从同一候选池派生；
- verifier preference pairs 如何在没有人工标注的情况下构造；
- TRL SFTTrainer 与 DPOTrainer 分别接收什么数据；
- 如何使用统一预算比较正确率、输出长度和推理成本；
- 如何用固定回归集合识别数学专项训练造成的通用能力变化；
- 何时值得执行 8B 验证，何时应按停止规则结项。

项目最终只保留以下核心产物：

```text
一份实验卡
一份 Teacher generation YAML
一份带 fingerprint 的 Teacher candidate Dataset
三类 SFT Dataset 与一类 preference Dataset
4B 三类蒸馏 adapter 与一个 DPO adapter
条件式 8B 结果
TensorBoard 日志与 LightEval 结果目录
一个分析 notebook
一份专题总结
```

当这些产物能够相互追溯、核心实验可以从 YAML 重新启动、主要学习问题得到证据支持的回答时，本专题结项。
