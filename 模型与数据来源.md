# 模型与数据来源说明

## 1. 文档目的

本文是 [LLM 自学项目计划](./LLM自学计划.md)、[模型蒸馏专题学习计划](./蒸馏学习计划.md) 与 [模型架构迁移与研究学习计划](./架构迁移与研究计划.md) 的配套文档，用于说明计划中使用的模型、代码、Tokenizer 和训练数据分别来自哪里，以及它们在各阶段之间如何流转。外部资源的来源与 revision，以及模型和数据的许可信息以本文为权威记录；各计划文件只保留执行所需的冻结快照。

本文记录模型、代码、Tokenizer、数据和 checkpoint 的来源关系，覆盖以下问题：

- 当前模型是从随机参数开始，还是从公开权重开始；
- 当前 checkpoint 是由哪个阶段产生的；
- 数据是公开下载，还是由 Teacher 模型生成；
- Pretrain、SFT、DPO、蒸馏和 RL 数据各自服务于哪个阶段；
- 代码、模型、Tokenizer 和数据如何保持版本一致。

本文调研日期为 **2026-08-01**。文中 revision 构成本项目的初始版本基线。revision、commit、LFS 与文件 hash 统一显示前 7 位；执行时由 Hub、Git 和运行工具解析完整值并写入机器生成的锁定记录，不在计划正文中展开。后续升级单独记录新的 revision 和变更原因。

---

## 2. 总体来源与流转

本项目使用四类模型来源：

1. **从零训练的教学模型**：Bigram LM、TinyGPT 和 MiniMind；
2. **公开 Base 权重上的后训练模型**：Qwen3；
3. **蒸馏 Teacher**：公开的 gpt-oss-20b 权重；
4. **架构迁移与研究模型**：公开 GPT-2 Medium checkpoint，以及按 OLMo 3 官方预设随机初始化的 100M、190M 和 370M 模型。

整体流转如下：

```text
Tiny Shakespeare
├→ 字符级 Tokenizer → 随机初始化 Bigram LM → 随机初始化 TinyGPT
└→ 可选 Byte-level BPE 教学对照

MiniMind-3 官方代码 + 官方 Tokenizer
→ 随机初始化 MiniMind
→ 自己训练的 Pretrain checkpoint
├→ Full SFT checkpoint
│   ├→ DPO checkpoint
│   └→ Distillation Student checkpoint
└→ LoRA SFT adapter

Qwen3 官方权重
├→ 4B-Base + GSM8K → 正式 QLoRA SFT
├→ 0.6B + UltraFeedback → DPO 框架验证
├→ 1.7B / 4B / 条件式 8B-Base + Teacher 数据 → 蒸馏 SFT → Verifier-filtered DPO
└→ 4B 数学 SFT / 数学 DPO checkpoint + GSM8K → 可选 GRPO

GPT-2 Medium 官方 checkpoint
→ 本地兼容 MHA checkpoint
→ SDPA / KV Cache 实现
├→ MHA 继续预训练对照
├→ GQA-8 转换与恢复训练
└→ GQA-4 转换与恢复训练

OLMo-core OLMo 3 官方预设
→ 随机初始化 100M 冒烟模型
→ 随机初始化 190M MHA / GQA / MQA 筛选模型
→ 随机初始化 370M MHA / 最佳 GQA 正式模型
```

核心原则：

> “从零训练”在本文中指模型参数随机初始化。Tokenizer、训练代码和数据采用下文指定的来源。

> 正式实验优先使用论文或主流开源项目采用的公开数据、官方标签、固定 split 和现成评估器。

本项目不安排大规模人工标注、逐条数据清洗或手工修正 Teacher 输出。无法解析、输出截断或重复的生成结果不进入派生数据集；可解析的错误候选保留在原始候选池，可作为 DPO rejected response。少量只读抽查只用于发现流水线错误。

---

## 3. 模型从哪里来

### 3.1 Bigram LM

#### 来源

Bigram LM 由本项目自行编写，参数采用随机初始化：

```python
nn.Embedding(vocab_size, vocab_size)
```

#### 初始化方式

- 模型参数：随机初始化；
- vocabulary：从训练文本统计得到；
- Tokenizer：自行编写的字符级 Tokenizer；
- checkpoint：训练后由本项目自行产生。

#### 用途

本阶段聚焦 Dataset、DataLoader、forward、loss、backward、optimizer、validation 和 checkpoint。阶段验收以训练流程为准。

#### 可选 BPE 拓展

阶段 1 验收后，可使用 [Hugging Face Tokenizers](https://huggingface.co/docs/tokenizers/) 的 Byte-level BPE 实现，在同一 Tiny Shakespeare train split 上临时训练 vocabulary size 为 512 和 2,000 的两个 tokenizer。它们只用于与字符级 tokenizer 比较切分、序列长度和 encode / decode 可逆性，不进入 TinyGPT、MiniMind 或后续模型的训练链路。

---

### 3.2 TinyGPT

#### 来源

TinyGPT 的模型结构由本项目自行实现，参数采用随机初始化。

参考资料：

- [Karpathy nanoGPT](https://github.com/karpathy/nanoGPT)

#### 初始化方式

- 模型参数：随机初始化；
- Tokenizer：第一轮继续使用字符级 Tokenizer；
- checkpoint：训练后由本项目自行产生；
- 参数规模：控制在 1M～10M。

#### 用途

本阶段聚焦 Decoder-only Transformer 的完整 forward，阶段验收以代码正确和 loss 下降为准。

---

### 3.3 MiniMind

#### 官方代码来源

- GitHub：[jingyaogong/minimind](https://github.com/jingyaogong/minimind)
- 本次调研 revision：`89d674b`

当前主线为 **MiniMind-3**：

- Dense：约 64M 参数；
- MoE：约 198M 总参数、64M active parameters；
- 当前模型结构、Tokenizer、数据命名和训练流程与旧版 MiniMind2、MiniMind-v1 分开管理。

本项目统一采用 MiniMind-3 主线。旧版资源归档标识包括：

- `pretrain_hq.jsonl`；
- `sft_mini_512.jsonl`；
- MiniMind2 / MiniMind-v1 权重；
- 旧版 Tokenizer。

当前 MiniMind-3 训练脚本与当前 Tokenizer、t2t 数据文件配套使用。

#### Tokenizer 来源

MiniMind 阶段使用官方代码仓库 `model/` 目录中的 Tokenizer 文件：

- `model/tokenizer.json`
- `model/tokenizer_config.json`

以下阶段使用同一 Tokenizer：

```text
Pretrain
→ SFT
→ LoRA
→ DPO
→ Logit Distillation
```

上述阶段固定使用同一 Tokenizer，确保 token ID、embedding 和输出层保持一致。

#### 参数来源

MiniMind 的主要学习路线从随机参数开始：

```text
MiniMindConfig
→ 随机初始化
→ 自己训练 Pretrain checkpoint
→ 自己训练 SFT / LoRA / DPO / Distillation
```

官方公开权重用于：

- 检查推理环境是否正常；
- 对比自己训练的模型；
- 在蒸馏阶段作为可选 Teacher；
- 在某一阶段训练失败时排查问题。

官方权重来源：

- Transformers 格式：[jingyaogong/minimind-3](https://huggingface.co/jingyaogong/minimind-3)
- 本次调研 revision：`f92512d`
- PyTorch 权重：[jingyaogong/minimind-3-pytorch](https://huggingface.co/jingyaogong/minimind-3-pytorch)
- PyTorch 权重 revision：`edba70e`

实验记录明确标注权重来源：

```text
official checkpoint
self-trained checkpoint
```

---

### 3.4 Qwen 正式后训练模型

#### 模型代际

本项目使用以下 Qwen3 Base 与后训练权重：

| 用途 | 模型 ID | 本次调研 revision |
| --- | --- | --- |
| SFT 开发与可选 continued pretraining | [`Qwen/Qwen3-0.6B-Base`](https://huggingface.co/Qwen/Qwen3-0.6B-Base) | `da87bfb` |
| DPO 框架验证 | [`Qwen/Qwen3-0.6B`](https://huggingface.co/Qwen/Qwen3-0.6B) | `c1899de` |
| MiniMind Sequence-level Distillation Teacher | [`Qwen/Qwen3-4B`](https://huggingface.co/Qwen/Qwen3-4B) | `1cfa9a7` |
| SFT / 蒸馏流程开发 | [`Qwen/Qwen3-1.7B-Base`](https://huggingface.co/Qwen/Qwen3-1.7B-Base) | `ea980cb` |
| 正式 SFT 与蒸馏主实验 | [`Qwen/Qwen3-4B-Base`](https://huggingface.co/Qwen/Qwen3-4B-Base) | `906bfd4` |
| 条件式蒸馏规模验证 | [`Qwen/Qwen3-8B-Base`](https://huggingface.co/Qwen/Qwen3-8B-Base) | `49e3418` |
| 蒸馏后续扩展 | [`Qwen/Qwen3-14B-Base`](https://huggingface.co/Qwen/Qwen3-14B-Base) | `0b0bd37` |

Qwen 模型及其 revision 统一从 Hugging Face 的 Qwen 官方组织获取。

Qwen3 官方说明：

- [Qwen3 官方发布说明](https://qwenlm.github.io/blog/qwen3/)
- Qwen3 Dense 系列包含 0.6B、1.7B、4B、8B、14B 和 32B；
- 上述 Qwen3 模型使用 Apache License 2.0。

#### 各训练分支

正式 SFT、蒸馏和可选 GRPO 以 Base 或本项目产生的 checkpoint 为起点；DPO 框架验证独立使用已经后训练的 0.6B 模型。各分支为：

```text
4B-Base → GSM8K QLoRA SFT
0.6B → UltraFeedback DPO 框架验证
1.7B / 4B / 条件式 8B-Base → 蒸馏 SFT → Verifier-filtered DPO
4B GSM8K SFT checkpoint / 4B 数学 DPO checkpoint → 可选 GRPO
```

阶段 10 的 `Qwen/Qwen3-0.6B` UltraFeedback DPO adapter 只验证 TRL 数据流，不作为数学 GRPO 的初始化模型。可选 GRPO 默认从阶段 10 的 4B GSM8K SFT checkpoint 开始；完成蒸馏专题后，也可以从阶段 D5 的 4B 数学 DPO checkpoint 开始。

Base 权重提供清晰的训练起点，使 SFT、蒸馏和 RL 带来的行为变化可以独立观察。

已经后训练的 Qwen3 模型用于：

- 与自己训练结果对比；
- 生成 sequence-level distillation 数据；
- 作为可选 Teacher。

阶段 8 的 MiniMind Sequence-level Distillation 固定使用 `Qwen/Qwen3-4B`。该模型只处理 100 条教学 prompts，不参与 MiniMind Logit Distillation。

可选 Qwen Teacher：

- [`Qwen/Qwen3-8B`](https://huggingface.co/Qwen/Qwen3-8B)
- 本次调研 revision：`b968826`

#### Qwen checkpoint 的记录

```text
Base model ID + revision
→ adapter 或训练 checkpoint
→ evaluation result
```

每个阶段保留 Base 模型标识，并按实际产物记录 adapter 或 checkpoint 路径。只有部署或兼容性测试需要时才生成合并权重。

---

### 3.5 gpt-oss-20b

gpt-oss-20b 在 [模型蒸馏专题学习计划](./蒸馏学习计划.md) 中定位为正式 Teacher。

#### 官方来源

- Hugging Face：[`openai/gpt-oss-20b`](https://huggingface.co/openai/gpt-oss-20b)
- GitHub：[openai/gpt-oss](https://github.com/openai/gpt-oss)
- 本次调研 revision：`6cee5e8`
- 协议：Apache License 2.0；模型使用同时遵循项目 usage policy

#### 本项目中的用途

- 生成 sequence-level distillation 回答；
- 生成候选 SFT 数据；
- 对比不同 Teacher 的输出；
- 观察 reasoning model 的输出格式。

#### 使用边界

- MiniMind token-level / logit distillation 使用 MiniMind Teacher；
- MiniMind 参数保持独立初始化；
- Qwen reference model 使用 DPO 开始前的同一 Qwen checkpoint；
- 正式微调从 Qwen3 Base 开始。

Qwen 与 gpt-oss 使用不同的 Tokenizer。二者之间采用 response-level / sequence-level distillation。

---

### 3.6 GPT-2 Medium 与 OLMo 3 架构研究模型

#### GPT-2 Medium

- Hugging Face：[`openai-community/gpt2-medium`](https://huggingface.co/openai-community/gpt2-medium)
- 本次调研 revision：`6dcaa7a`
- 参数来源：OpenAI GPT-2 Medium 公开 checkpoint
- Tokenizer：同一仓库的 GPT-2 byte-level BPE，vocabulary size 50,257
- 官方配置：24 层、hidden size 1024、16 个 attention heads、context length 1024
- 协议：Modified MIT License

本项目从冻结版本的 Transformers GPT-2 源码派生本地薄改版本，保留官方非 attention 组件、模块层级和参数命名，再进行 SDPA、基础动态 KV Cache 与 GQA 改造。GQA checkpoint 由原 MHA checkpoint 的 K/V heads 分组平均得到；具体训练预算与对照设计见架构计划。

```text
official GPT-2 Medium
→ compatible local MHA
├→ MHA + SDPA + KV Cache
├→ MHA continual-pretraining control
├→ GQA-8 conversion → recovery checkpoint
└→ GQA-4 conversion → recovery checkpoint
```

官方 checkpoint 始终保留为 reference，不被本地转换脚本覆盖。

#### OLMo 3

- 训练框架：[allenai/OLMo-core](https://github.com/allenai/OLMo-core)
- 本次调研 commit：`064b172`
- 配置文档：[OLMo-core `TransformerConfig`](https://olmo-core.readthedocs.io/en/stable/nn/transformer.html)
- 参数来源：随机初始化
- 官方预设：`olmo3_100M`、`olmo3_190M`、`olmo3_370M`

100M 用于验证训练和 checkpoint 恢复，190M 用于筛选 attention 变体，370M 用于正式验证胜出方案。OLMo 3 预设负责模型宽度、层数、FFN、初始化方法、QK-Norm 和 sliding-window pattern；KV-head 配置、初始化配对审计和性能实验设计见架构计划。

---

### 3.7 蒸馏数据生成工具

Qwen 蒸馏专题固定复用以下官方代码来源：

| 工具 | 官方来源 | 本次调研 revision | 本项目用途 |
| --- | --- | --- | --- |
| Open R1 | [huggingface/open-r1](https://github.com/huggingface/open-r1) | `1416fa0` | 复用 `generate.py` 的合成数据入口 |
| Distilabel | [argilla-io/distilabel](https://github.com/argilla-io/distilabel) | `313fac8` | 批量生成、步骤缓存与中断恢复 |
| openai-harmony | [openai/harmony](https://github.com/openai/harmony) | `abd677f` | 解析 gpt-oss Harmony response format |

三项 revision 与依赖锁定文件共同构成 Teacher 数据生成基线。后续升级单独记录兼容性原因，已经生成的数据继续保留原始工具 revision。

---

## 4. 数据从哪里来

### 4.1 Bigram LM 与 TinyGPT 数据

#### 默认数据

默认使用 Tiny Shakespeare：

- 原始来源：[Karpathy char-rnn Tiny Shakespeare](https://github.com/karpathy/char-rnn/tree/master/data/tinyshakespeare)
- 固定 commit：`6f9487a`
- 固定文件：[`data/tinyshakespeare/input.txt`](https://github.com/karpathy/char-rnn/blob/6f9487a/data/tinyshakespeare/input.txt)
- 原始文件 SHA-256 前缀：`86c4e6a`
- 参考预处理：[nanoGPT `shakespeare_char`](https://github.com/karpathy/nanoGPT/tree/3adf61e/data/shakespeare_char)，commit `3adf61e`
- 数据内容：Shakespeare 作品的一个小型文本子集；
- 数据形式：单个 `input.txt` 文本文件。

#### 本地处理

本项目自行完成：

1. 读取原始文本；
2. 统计字符 vocabulary；
3. 编码为 token ID；
4. 沿用 nanoGPT 的确定性顺序划分：前 90% 为 train，后 10% 为 validation；
5. 保存切分位置和原始文件哈希。

第一轮固定使用 Tiny Shakespeare。字符级模型聚焦训练流程，训练语言保持不变。

可选 BPE 拓展直接复用上述 train / validation split，不下载新语料，不增加清洗、标注或人工改写。BPE vocabulary 只作为阶段 1 的临时教学产物。

---

### 4.2 MiniMind 官方数据

#### 数据仓库

- Hugging Face：[jingyaogong/minimind_dataset](https://huggingface.co/datasets/jingyaogong/minimind_dataset)
- 本次调研 revision：`312afb4`

当前数据卡同时标注 Apache-2.0 和 CC-BY-NC-2.0。该数据集聚合了公开数据、整理数据和合成数据，各子来源遵循各自的许可条件。本项目按学习用途使用，其他用途逐项核对来源协议。

MiniMind-3 当前主线数据来源包括公开通用文本、指令数据、对话数据和合成数据。官方数据卡列出的主要来源包括匠数大模型数据集、Magpie-Align、R1-Distill-SFT、COIG、Step-3.5-Flash-SFT，以及由 Qwen3 系列生成的部分 reasoning 和 tool-calling 数据。

#### 本项目使用的文件

| 阶段 | 文件 | 数据形式 | 用途 |
| ---: | --- | --- | --- |
| 4 | `pretrain_t2t_mini.jsonl` | `{"text": "..."}` | MiniMind 从头预训练 |
| 5 | `sft_t2t_mini.jsonl` | `conversations` 多轮消息 | Full SFT |
| 6 | `sft_t2t_mini.jsonl` 的同一训练子集 | `conversations` 多轮消息 | LoRA 与 Full SFT 对照 |
| 7 | `dpo.jsonl` 固定子集 | chosen / rejected | DPO |
| 8 | `pretrain_t2t_mini.jsonl` 或 `sft_t2t_mini.jsonl` 的固定 prompt 子集 | text / conversations | Logit Distillation |

这些文件直接沿用官方 schema 与训练脚本。教学子集统一采用确定性规则：阶段 4 从 `pretrain_t2t_mini.jsonl` 按原始顺序取前 256 条；阶段 5 从 `sft_t2t_mini.jsonl` 以 seed `42` 打乱后取前 100 条；阶段 7 从 `dpo.jsonl` 以 seed `42` 打乱后取前 64 条。Logit Distillation 选定数据文件后也以 seed `42` 生成一次固定索引，全部 CE / KD 对照复用。每份索引保存 Dataset revision、fingerprint 与原始 row ID。项目只做字段校验和少量只读抽查，不人工挑选、清洗或改写样本。

#### 固定评估基线

MiniMind 客观评估沿用官方项目当前采用的微型模型横评方案：

- 评测框架：[EleutherAI/lm-evaluation-harness](https://github.com/EleutherAI/lm-evaluation-harness)
- 固定版本：`v0.4.12`，commit `6d64254`
- 固定任务：`ceval-valid`、`cmmlu`、`arc_easy`、`piqa`、`openbookqa`、`hellaswag`、`social_iqa`
- 模型入口：MiniMind 官方 `scripts/convert_model.py` 转换后的 Transformers checkpoint 与 lm-eval `hf` backend

七项任务的底层数据同时固定为：

| lm-eval task | Hugging Face 数据集 | 固定 revision |
| --- | --- | --- |
| `ceval-valid` | [`ceval/ceval-exam`](https://huggingface.co/datasets/ceval/ceval-exam) | `617524a` |
| `cmmlu` | [`haonan-li/cmmlu`](https://huggingface.co/datasets/haonan-li/cmmlu) | `efcc940` |
| `arc_easy` | [`allenai/ai2_arc`](https://huggingface.co/datasets/allenai/ai2_arc) | `210d026` |
| `piqa` | [`baber/piqa`](https://huggingface.co/datasets/baber/piqa) | `142f6d7` |
| `openbookqa` | [`allenai/openbookqa`](https://huggingface.co/datasets/allenai/openbookqa) | `388097e` |
| `hellaswag` | [`Rowan/hellaswag`](https://huggingface.co/datasets/Rowan/hellaswag) | `218ec52` |
| `social_iqa` | [`allenai/social_i_qa`](https://huggingface.co/datasets/allenai/social_i_qa) | `8835ceb` |

环境准备脚本只为冻结的上游 task YAML 注入对应的 `dataset_kwargs.revision`，并通过 lm-eval 的本地 task 路径加载；prompt、split、few-shot 和 metric 保持 `v0.4.12` 原样。生成的 task 目录记录哈希，避免把“固定 task 配置”误当成“固定 Dataset revision”。Pretrain checkpoint 不应用 chat template；SFT、LoRA 与 DPO checkpoint 使用 MiniMind 官方 chat template。执行时继续保存 Dataset fingerprint、task version、few-shot 设置与结果 JSON。该套件只用于阶段间回归检查，不参与训练、样本选择或超参数调整。

固定生成观察 prompts 直接取自 MiniMind 官方 README：

1. 为什么天空是蓝色的
2. 解释什么是机器学习
3. 推荐一些中国的美食

三条 prompts 只用于 checkpoint 与 decoding 行为观察，不进行人工评分。

---

### 4.3 MiniMind 蒸馏数据

#### Sequence-level Distillation

固定 prompt 来源为 4.4 节记录的 [`openai/gsm8k`](https://huggingface.co/datasets/openai/gsm8k) `main` 配置。数据由本项目自动生成：

```text
openai/gsm8k（main）正式训练 prompts
→ 按项目级 `gsm8k_split_v1.json` 排除固定的 500 条 development
→ 读取正式训练区的前 100 个固定 row ID
→ Qwen/Qwen3-4B 生成 reasoning 与 final answer
→ Math-Verify 对照官方 gold answer 自动验证
→ 验证通过的 response 进入 MiniMind Student SFT
```

`gsm8k_split_v1.json` 在阶段 8 开始时首次生成：为官方 train 添加原始 row ID，以 seed `42` 打乱一次，前 500 条作为 development，其余 6,973 条作为正式训练区，正式训练区前 100 条同时记为 `smoke_100`。该清单随后由阶段 10、蒸馏专题和可选 GRPO 只读复用，不重新抽样。验证失败的 Teacher response 不进入训练集，也不进行人工改写、补标或筛选。模型 revision、split 清单哈希与 generation config 随实验配置保存。

#### Logit Distillation

logits 由 Teacher 在训练过程中执行 forward 产生。

要求：

- Teacher 与 Student 使用完全相同的 Tokenizer；
- 输入样本来自 `pretrain_t2t_mini.jsonl` 或 `sft_t2t_mini.jsonl`；
- Teacher logits、Student logits 和 labels 对应相同 token 位置。

MiniMind Logit Distillation 在更大的 Dense MiniMind 或 MiniMind MoE Teacher、MiniMind 64M Dense Student，以及上述两类官方数据文件的范围内固定一组配置；全部 CE / KD 对照复用该配置。阶段 8 的 Sequence-level Distillation 固定使用 Qwen3-4B；后续 Qwen 蒸馏专题另行使用 gpt-oss-20b。

---

### 4.4 Qwen 正式项目数据

Qwen 正式任务使用固定的公开数据基线。GSM8K 服务于数学 SFT、蒸馏和可选 GRPO；UltraFeedback 只服务于独立的 DPO 框架验证；OpenWebMath 只服务于可选 continued pretraining。

#### 框架开发与 DPO 验证

SFT 开发直接复用 GSM8K 的小规模训练子集，不建立另一套演示数据。DPO 框架验证固定使用：

- Hugging Face：[`trl-lib/ultrafeedback_binarized`](https://huggingface.co/datasets/trl-lib/ultrafeedback_binarized)
- 本次调研 revision：`47124cb`
- 用途：在 `Qwen/Qwen3-0.6B` 上验证 TRL preference 数据流
- 范围：train split 按 seed `42` 打乱后抽取 1,000 条，完成一次短训练
- 协议说明：该镜像数据卡未单独声明 license；它来源于公开 UltraFeedback / HuggingFaceH4 处理链，学习用途之外需继续核对上游数据条款

该分支不构造 GSM8K preference pairs，不进入数学任务结论，也不运行完整回归评估。

#### 正式数学任务

阶段 10 与后续蒸馏专题统一采用 GSM8K 数学推理任务。

#### GSM8K

- Hugging Face：[`openai/gsm8k`](https://huggingface.co/datasets/openai/gsm8k)
- config：`main`
- 本次调研 revision：`740312a`
- 协议：MIT
- 官方 train：7,473 条
- 官方 test：1,319 条
- 论文：[Training Verifiers to Solve Math Word Problems](https://arxiv.org/abs/2110.14168)

项目统一读取阶段 8 生成的 `gsm8k_split_v1.json`：官方 train 经 seed `42` 的单次确定性打乱后，前 500 条为 development，其余 6,973 条用于普通 SFT 和 Teacher 数据生成。清单保存原始 row ID、Dataset revision、fingerprint 与自身哈希；阶段 10 只核验并复用，不重新抽样。官方 test 始终保持只读。

字段直接采用公开版本：

```text
question
answer = rationale + "####" + gold answer
```

普通 SFT 使用公开 rationale 和 gold answer。蒸馏、专题内的 verifier-filtered DPO 与可选 GRPO 使用同一批训练 prompts，并通过 gold answer 自动产生监督、偏好信号或 reward。

#### MATH-500

- Hugging Face：[`HuggingFaceH4/MATH-500`](https://huggingface.co/datasets/HuggingFaceH4/MATH-500)
- 本次调研 revision：`6e4ed1a`
- 协议：MIT（上游 OpenAI PRM800K 仓库）
- 数量：500 条
- 来源：OpenAI 在 *Let's Verify Step by Step* 工作中整理的 MATH 子集
- 用途：高难度泛化评估

MATH-500 不用于训练、Teacher 生成、超参数选择或 checkpoint 筛选。

#### 可选 continued pretraining 数据

- 训练配方：[Hugging Face Alignment Handbook](https://github.com/huggingface/alignment-handbook)，commit `1de1fc9`
- Hugging Face：[`open-web-math/open-web-math`](https://huggingface.co/datasets/open-web-math/open-web-math)
- 本次调研 revision：`fde8ef8`
- 协议：ODC-By 1.0，并遵循 Common Crawl Terms of Use
- 数据规模：约 630 万文档、147 亿 tokens
- 用途：阶段 10 的可选数学领域 continued pretraining 旁路

该数据集已经完成数学内容筛选和去重。项目使用现成配方进行固定子集抽样与训练，不抓取网页、不重跑全量过滤和去重。

#### 自动评估工具

- 数学答案抽取与等价判定：[Hugging Face Math-Verify](https://github.com/huggingface/Math-Verify)
- 标准任务执行与结果汇总：[Hugging Face LightEval](https://github.com/huggingface/lighteval)

Math-Verify 已提供 GSM8K 与 MATH-500 支持。LightEval 提供现成任务和多种推理 backend。项目固定工具版本后直接调用标准任务，不编写自定义 benchmark evaluator。

#### 任务更换规则

核心计划不更换 GSM8K。后续若进入代码生成、SQL 或 tool calling 专题，只选择具有公开 gold labels、官方 split 和可执行 verifier 的公认数据集，并在本文件中记录 revision 与协议。缺少这些条件的任务不进入正式计划。

---

### 4.5 Qwen 蒸馏数据

Qwen 蒸馏主线采用 sequence-level distillation，并从同一批多候选中构造 preference distillation 数据：

```text
GSM8K 正式训练 prompts
→ gpt-oss-20b 生成多候选回答
→ Math-Verify 自动验证与去重
├→ 每个 prompt 按固定 candidate ID 选择一个正确候选
│   └→ Answer-only / Compressed rationale / Full reasoning
└→ 同一 prompt 的可解析正确 / 错误候选
    └→ Verifier-filtered preference pairs
→ Qwen3-1.7B 完成流程调试
→ Qwen3-4B 完成正式消融
→ Qwen3-8B 按条件验证胜出方案
→ Qwen3-14B 仅作为条件式扩展
```

每条生成数据至少记录：

- prompt；
- teacher model ID；
- teacher revision；
- system prompt；
- generation config；
- 原始 response；
- GSM8K gold answer；
- 验证结果；
- 是否进入训练集。

所有候选保留在原始候选池并标记验证结果。无法解析、输出截断或重复的候选不进入派生训练数据；可解析的错误候选只用于构造 verifier-filtered DPO 的 rejected response，不进入 SFT。只读抽查不超过固定的小样本数量，不产生新标签，也不修改公开 gold answer。

token-level logit distillation 限定在使用同一 Tokenizer 的 Teacher 与 Student 之间。

核心路线只使用 verifier-filtered preference pairs。Teacher-ranked preference 数据属于蒸馏专题的条件式扩展，复用同一候选池与固定自动 judge，不增加人工排序。

---

### 4.6 Evaluation 数据

数学任务 Evaluation 数据固定为：

1. GSM8K train 中按 seed `42` 抽取的 500 条 development；
2. GSM8K 官方 1,319 条 test；
3. MATH-500 的 500 条高难度泛化题。

数据隔离规则：

- evaluation 数据独立于 SFT、DPO chosen/rejected 构造和蒸馏数据；
- Teacher 生成范围限定为训练 prompts，evaluation prompts 独立保留；
- 所有进入数学结论的 checkpoint 使用同一份 evaluation 数据；
- generation config 保持一致；
- GSM8K test 与 MATH-500 不用于训练、超参数选择和 checkpoint 筛选；
- 训练集发生变化时，重新检查 evaluation 的独立性。

通用回归评估使用下列公开数据。它们全程只读，只观察专项训练前后的能力变化，不参与 checkpoint 选择、超参数调整或训练数据生成。

| 维度 | 数据集 | 本次调研 revision | 协议 |
| --- | --- | --- | --- |
| 指令遵循 | [`google/IFEval`](https://huggingface.co/datasets/google/IFEval) | `966cd89` | Apache-2.0 |
| 通用知识与推理 | [`cais/mmlu`](https://huggingface.co/datasets/cais/mmlu) | `c30699e` | MIT |
| 真实性风险信号 | [`truthfulqa/truthful_qa`](https://huggingface.co/datasets/truthfulqa/truthful_qa) 的 multiple-choice 配置 | `741b827` | Apache-2.0 |

执行时使用 LightEval 现成任务。精确 task ID、metric 与 LightEval revision 在首次运行时通过 `lighteval tasks list` 和 `lighteval tasks inspect` 冻结到机器可读配置，避免在计划正文中固化可能变化的任务别名。

---

### 4.7 架构迁移与研究数据

#### GPT-2 连续预训练数据

- Hugging Face：[`Skylion007/openwebtext`](https://huggingface.co/datasets/Skylion007/openwebtext)
- 本次调研 revision：`79d93d7`
- 预处理脚本：[nanoGPT `data/openwebtext/prepare.py`](https://github.com/karpathy/nanoGPT/blob/master/data/openwebtext/prepare.py)
- nanoGPT commit：`3adf61e`
- Tokenizer：GPT-2 BPE
- 格式：连续 `uint16` token stream，每个文档后追加 GPT-2 EOT token `50256`
- 协议：OpenWebText 数据卡与上游内容许可分别适用

项目执行 nanoGPT 现成脚本，并固定以下划分：

```text
train_test_split(test_size=0.0005, seed=2357, shuffle=True)
```

| 文件 | 文档数 | Token 数 | 用途 |
| --- | ---: | ---: | --- |
| `train.bin` | 8,009,762 | 约 9,035,582,198 | 各架构共用的连续训练流 |
| `val.bin` | 4,007 | 约 4,434,897 | 固定同分布 validation，只读 |

各架构读取相同数据位置。训练 token 预算、update 检查点和 validation 窗口由架构计划统一规定。

外部分布 validation 使用 [`Salesforce/wikitext`](https://huggingface.co/datasets/Salesforce/wikitext) 的 `wikitext-103-v1` 官方 validation split，本次调研 revision 为 `b08601e`，并使用 GPT-2 官方 Tokenizer 编码。它只评估最终关键 checkpoint，不参与模型选择。

预处理全程自动运行。项目不重新清洗、去重、重写、人工切分或标注文本。生成后记录 `train.bin`、`val.bin` 的字节数和 SHA-256。

#### OLMo 3 预训练数据

- Hugging Face：[`allenai/DataDecide-data-recipes`](https://huggingface.co/datasets/allenai/DataDecide-data-recipes)
- 本次调研 revision：`3baf34b`
- 数据配方：`fineweb-edu-dedup/v0`
- 预分词目录：`preprocessed/fineweb-edu-dedup/v0/gpt-neox-olmo-dolma-v1_5/`
- Tokenizer：[`allenai/gpt-neox-olmo-dolma-v1_5`](https://huggingface.co/allenai/gpt-neox-olmo-dolma-v1_5)
- Tokenizer revision：`8571ec7`
- 格式：OLMo-core 原生读取的 `.npy` token shards
- 协议：ODC-BY；同时遵循 Ai2 Responsible Use Guidelines

训练 manifest 固定为以下五个文件，并严格保持表中顺序：

| 顺序 | 文件 | Token 数 |
| ---: | --- | ---: |
| 1 | `part-000-00000.npy` | 1,073,741,783 |
| 2 | `part-000-00001.npy` | 506,924,995 |
| 3 | `part-001-00000.npy` | 1,073,740,304 |
| 4 | `part-001-00001.npy` | 610,967,463 |
| 5 | `part-002-00000.npy` | 797,578,124 |
|  | **合计** | **4,062,952,669** |

Validation 固定使用独立的 `part-003-00000.npy`，不进入训练 manifest。具体读取窗口、数据加载配置和各规模训练预算由架构计划统一规定。

DataDecide 已提供完成质量筛选、去重和预分词的数据配方。本项目不下载原始网页、不重跑过滤器、不重新去重、不重新分词。manifest 只记录文件路径、Hub revision、LFS hash、token 数与训练/验证用途。

OLMo 下游检查固定使用 [allenai/olmes](https://github.com/allenai/olmes) commit `5a51f50` 的 `core_9mcqa::olmes` 与 `mmlu::olmes`。两者覆盖 DataDecide 采用的标准 10 项多项选择任务：MMLU、HellaSwag、ARC-Challenge、ARC-Easy、PIQA、CommonsenseQA、Social IQa、OpenBookQA、BoolQ 和 Winogrande。190M 只评估 MHA baseline 与 validation loss 胜出比例，370M 评估全部正式运行的最终 checkpoint。项目不制作 gold labels，也不实现第二套 benchmark evaluator。

#### 架构专题条件式扩展来源

后训练迁移固定采用 AllenAI 的公开基线：

- 数据：[`allenai/tulu-3-sft-mixture`](https://huggingface.co/datasets/allenai/tulu-3-sft-mixture)，revision `b14afda`，ODC-BY；
- 训练入口：[allenai/open-instruct](https://github.com/allenai/open-instruct)，commit `b18cfc5`；
- 格式：直接读取公开 messages 和固定 Tülu chat template，对 370M MHA baseline 与最佳 GQA 使用同一确定性子集，不重写或补充标签。

长上下文吞吐扩展不引入新数据集。输入固定取自 DataDecide validation shard `part-003-00000.npy`：从核心 validation 窗口结束位置 token offset `4,194,304` 开始，按模型实际支持的 context length 截取一组连续 token IDs；全部 checkpoint 使用同一索引和 generation config。该输入只测量 prefill、decode、显存和 KV Cache，不用于质量结论或模型选择。

---

## 5. 来源记录

上游仓库持续更新，同一名称在不同 revision 下对应不同内容。每项外部资源记录以下字段：

| 字段 | 内容 |
| --- | --- |
| 名称与类型 | 模型、数据、Tokenizer 或代码 |
| 官方地址 | GitHub 或 Hugging Face |
| Revision | commit SHA 或 Hub revision |
| 协议 | 模型或数据卡标注的 license |
| 本地信息 | 下载日期、保存路径和文件哈希 |
| 实验关系 | 使用该资源的训练阶段和配置 |

数据记录同时包含 split 方法、split seed 和转换脚本的 Git commit。Teacher 生成数据同时记录 Teacher revision 和 generation config。

Git 保存来源清单、下载与转换脚本、数据 schema、split 索引、配置和实验记录。模型权重、完整数据集、训练 checkpoint 和平台缓存统一存放在外部资源目录。

---

## 6. 本项目的最终来源决策

| 项目阶段 | 模型来源 | 数据来源 |
| --- | --- | --- |
| Bigram LM | 自行实现、随机初始化 | Karpathy Tiny Shakespeare |
| BPE 拓展（可选） | Hugging Face Tokenizers Byte-level BPE；临时训练 | 同一 Tiny Shakespeare train / validation split |
| TinyGPT | 自行实现、随机初始化 | 同一 Tiny Shakespeare split |
| MiniMind Pretrain | MiniMind-3 官方代码与 Tokenizer、随机初始化 | `pretrain_t2t_mini.jsonl` |
| MiniMind SFT / LoRA | 自己训练的 Pretrain checkpoint | `sft_t2t_mini.jsonl` |
| MiniMind DPO | 自己训练的 SFT checkpoint | `dpo.jsonl` 固定子集 |
| MiniMind 客观评估 | Pretrain / SFT / LoRA / DPO checkpoint | lm-eval 固定七任务套件，只读 |
| MiniMind Sequence-level Distillation | `Qwen/Qwen3-4B` → MiniMind Student | GSM8K 固定 100 条 prompts + 自动验证通过的 Teacher responses |
| MiniMind Logit Distillation | MiniMind Teacher → MiniMind Student | MiniMind 官方 prompt 子集 + Teacher logits |
| Qwen SFT 开发 | `Qwen/Qwen3-0.6B-Base` 或 `Qwen/Qwen3-1.7B-Base` | GSM8K train / development |
| Qwen 正式 SFT | `Qwen/Qwen3-4B-Base` | GSM8K train；GSM8K test、MATH-500 与冻结通用回归只读评估 |
| Qwen DPO 框架验证 | `Qwen/Qwen3-0.6B` | `trl-lib/ultrafeedback_binarized` 固定子集 |
| Qwen continued pretraining（可选） | `Qwen/Qwen3-0.6B-Base` | `open-web-math/open-web-math` 固定子集 |
| Qwen Distillation Teacher | `openai/gpt-oss-20b` | GSM8K 正式训练 prompts + 多候选 Teacher responses |
| Qwen Distillation 开发 | `Qwen/Qwen3-1.7B-Base` | GSM8K gold 自动验证后的蒸馏数据 |
| Qwen Distillation 主实验 | `Qwen/Qwen3-4B-Base` | 三类 SFT 数据 + 一类 verifier-filtered preference 数据；Teacher-ranked 为条件式扩展 |
| Qwen Distillation 规模验证（条件式） | `Qwen/Qwen3-8B-Base` | 胜出方案 + GSM8K、MATH-500 与冻结通用回归评估 |
| Qwen Distillation 后续扩展 | `Qwen/Qwen3-14B-Base` | 仅在专题完成后按条件复现胜出方案 |
| 可选 GRPO | 阶段 10 的 4B GSM8K SFT，或阶段 D5 的 4B 数学 DPO checkpoint | `gsm8k_split_v1.json` 训练 prompts + 在线 rollout + Math-Verify |
| GPT-2 架构迁移 | `openai-community/gpt2-medium` → 本地 MHA / GQA checkpoint | nanoGPT OpenWebText 固定 split + OpenWebText / WikiText-103 validation |
| OLMo 3 冒烟测试 | `olmo3_100M` 官方预设、随机初始化 | 固定 DataDecide manifest + 独立 validation shard |
| OLMo 3 候选筛选 | `olmo3_190M` 官方预设、随机初始化 | 同一 DataDecide manifest + 独立 validation shard |
| OLMo 3 正式验证 | `olmo3_370M` 官方预设、随机初始化 | 同一 DataDecide manifest + 独立 validation shard |
| OLMo 3 后训练迁移（条件式） | 370M MHA baseline / 最佳 GQA | `allenai/tulu-3-sft-mixture` 固定子集 + open-instruct |
| OLMo 3 长上下文吞吐（条件式） | 370M MHA baseline / 最佳 GQA | DataDecide validation shard 的固定连续 token 窗口 |

Qwen 数学 SFT、蒸馏和可选 GRPO 统一采用 GSM8K，DPO 框架验证独立采用 UltraFeedback。数学评估统一使用 Math-Verify 与 LightEval，通用回归统一使用冻结的 IFEval、MMLU 和 TruthfulQA LightEval 任务。架构专题统一使用冻结的预分词语料、OLMo-core evaluator 和固定系统 benchmark。
