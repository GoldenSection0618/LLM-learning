# 模型架构迁移与研究学习计划

> *当前 GQA 候选草案：从 GPT-2 Medium 的 checkpoint 改造进入 OLMo 3 的受控预训练实验，学习安全迁移、推理优化与架构归因。*

> **执行状态**　本文件保留为阶段 12 的候选设计，不是已冻结的执行计划。阶段 11 RL 系统学习结项后，重新调研 efficient-attention 方向，再决定是否采用本 GQA 草案或改用其他可控路线。

---

## 目录

1. [专题定位](#一专题定位)
2. [研究问题与技术路线](#二研究问题与技术路线)
3. [时间安排](#三时间安排)
4. [阶段执行计划](#四阶段执行计划)
5. [实验与评估规范](#五实验与评估规范)
6. [项目里程碑](#六项目里程碑)
7. [条件式扩展](#七条件式扩展)
8. [结项标准](#八结项标准)

---

## 一、专题定位

### 1.1　计划目标

本计划承接 [LLM 自学项目计划](./LLM自学计划.md) 的阶段 12，在 [模型蒸馏专题学习计划](./蒸馏学习计划.md) 与阶段 11 RL 系统学习完成后才决定是否启动。若采用本草案，专题围绕 Grouped-Query Attention（GQA）组织两项连续实验：

1. 在 GPT-2 Medium 上完成兼容实现、SDPA、KV Cache、MHA-to-GQA 权重迁移与短程恢复训练；
2. 在 OLMo 3 官方小规模预设上，从随机初始化比较不同 KV-head ratio；
3. 使用固定数据、固定训练 token 数和统一评估，区分迁移损失、训练恢复与架构差异；
4. 形成质量、显存、吞吐和解码效率的联合分析。

GPT-2 项目用于建立 checkpoint surgery 与推理实现直觉。OLMo 3 项目用于学习架构消融、变量控制和结论边界。两项实验共享同一个研究变量，训练起点和研究问题各自独立。

本专题以掌握方法、框架和研究流程为目标。核心计划保留一次完整迁移和一次完整受控研究，不追求论文级模型覆盖、超参数搜索或大规模 benchmark 榜单。

### 1.2　方案选择

OLMo-core 当前公开文档提供 `olmo3_100M`、`olmo3_190M` 与 `olmo3_370M` 等预设；公开文档中的 OLMo 2 小规模入口以 `olmo2_100M` 为主。专题统一采用 OLMo 3 预设：

| 用途 | 固定预设 | 选择原因 |
| --- | --- | --- |
| 代码验证 | `TransformerConfig.olmo3_100M(...)` | 直接使用官方最小预设，避免维护临时小模型配置 |
| 候选筛选 | `TransformerConfig.olmo3_190M(...)` | 官方预设，可低成本切换 `n_kv_heads` |
| 正式验证 | `TransformerConfig.olmo3_370M(...)` | 官方预设，与 190M 保持同一模型家族 |

该路线提高了基线的可复现性，减少自定义层数、宽度、FFN 比例、初始化和 sliding-window pattern 带来的配置维护。OLMo 2 风格的自定义 370M 不进入核心计划。

参考依据：

- [OLMo-core TransformerConfig 文档](https://olmo-core.readthedocs.io/en/stable/nn/transformer.html)
- [OLMo-core attention 配置文档](https://olmo-core.readthedocs.io/en/stable/nn/attention.html)
- [OLMo-core 研究者指南](https://olmo-core.readthedocs.io/en/stable/guides/all_in_one_for_researchers.html)

### 1.3　前置基础

进入本计划前，应已能够：

- 解释 Decoder-only Transformer、MHA、GQA、causal mask 与 KV Cache；
- 加载 Hugging Face 模型、Tokenizer 和 checkpoint；
- 阅读 PyTorch module、state dict 与 generation loop；
- 从头训练小型语言模型，并独立执行 validation；
- 使用配置文件、TensorBoard 和固定 seed 管理实验；
- 区分训练 token、参数规模、训练 FLOPs 与推理成本。

主计划阶段 2～4 已覆盖模型结构与预训练，阶段 9 已固化实验规范。本专题不重新讲解通用训练循环，也不重新搭建 checkpoint 管理和评估平台。

### 1.4　时间与资源

| 项目 | 安排 |
| --- | --- |
| 每周投入 | 35 小时 |
| 每周新内容 / 第三天复习 | 约 27 小时 / 约 8 小时 |
| 核心周期 | 6～7.5 个研究周 |
| 计划窗口 | 阶段 11 完成后；采用本草案时以 R1～R7.5 相对周次执行 |
| 核心投入 | 约 193～255 小时 |
| 计算资源 | 租用多卡高算力服务器 |
| 迁移模型 | `openai-community/gpt2-medium` |
| 架构研究模型 | OLMo 3 100M / 190M / 370M 官方预设 |
| 模型与数据平台 | Hugging Face |

每周投入只计算需要持续参与的学习、实现、检查、分析和记录时间。下载、训练和评估的无人值守等待时间单独记录。新增算力优先用于并行完成固定实验，不用于扩张核心矩阵。每天完成新内容后，在第三天（学习日记为 D 时的 D+2）安排约为首次学习时间 30% 的复习；复习时间已经计入阶段总投入和每周 35 小时。复习以脱离实现说明数据流、复查一项 correctness 证据和重新运行一个关键入口为主。

阶段运行完成后可以启动后续固定实验，对应第三天复习完成后才关闭阶段里程碑。完成 A3 和 B2 后使用实际主动投入校准尚未开始的阶段；偏离预算超过 20% 时才更新时间，不减少 B3 双 seed 正式验证、固定评估或复现要求。

### 1.5　专题主线

```text
GPT-2 Medium 官方 checkpoint
→ 兼容实现与逐层对齐
→ SDPA 与 KV Cache
→ MHA-to-GQA 权重转换
→ MHA / GQA 同预算的 100,663,296-token 连续训练
→ 质量与推理效率分析

OLMo 3 100M 官方预设
→ 数据、配置与训练冒烟测试
→ OLMo 3 190M 单 seed 候选筛选
→ OLMo 3 370M baseline / 最佳 GQA 双 seed 验证
→ token 视角与 GPU 时间视角联合分析
→ 专题结项
```

---

## 二、研究问题与技术路线

### 2.1　核心研究问题

专题回答两个相互衔接的问题：

> 将已有 MHA checkpoint 转换为 GQA 后，固定 token budget 的连续预训练能够恢复多少质量，并换取多少 KV Cache 与解码收益？

> 从随机初始化开始训练时，GQA 分组比例如何影响小型 Decoder 模型的质量、训练效率和推理成本？

GPT-2 实验只改变 attention 实现和 KV-head 数。OLMo 3 实验只改变 `n_kv_heads`。RoPE、QK-Norm、FFN、MoE、数据配比和长上下文扩展不进入核心变量。

### 2.2　模型基线

#### GPT-2 Medium

官方配置为 24 层、hidden size 1024、16 个 attention heads、1024 context length 和 50,257 vocabulary，参数量约 355M。权重和配置来自 [`openai-community/gpt2-medium`](https://huggingface.co/openai-community/gpt2-medium)。

Hugging Face GPT-2 已支持 `past_key_values`。本专题自行实现 KV Cache，用于理解缓存布局、causal mask 和逐 token 解码，并以官方实现作为正确性参考。

#### OLMo 3

OLMo-core 预设的 head 数会随规模变化：

| 预设 | Query heads | 核心候选 KV heads |
| --- | ---: | --- |
| OLMo 3 190M | 12 | 12、6、3、1 |
| OLMo 3 370M | 16 | 16、8、4、1 |

候选按 query-to-KV 分组比例命名：MHA、GQA-2、GQA-4、MQA。190M 胜出比例可以直接映射到 370M，无需修改模型宽度或 head dimension。

### 2.3　固定数据基线

本节保存专题执行时直接读取的冻结配置。[模型与数据来源](./模型与数据来源.md) 是外部资源来源与 revision，以及模型和数据许可信息的权威记录；文中的 hash 同样只显示前 7 位，完整值由执行工具写入机器生成的锁定记录。外部资源发生变更时，先更新来源文档，再同步本节的执行快照。

#### GPT-2 连续预训练

GPT-2 采用 nanoGPT 公开复现路线中的 OpenWebText baseline。OpenWebText 是 WebText 的开放复现，nanoGPT 已公开 GPT-2 系列在该数据上的训练、验证和 loss 参考值。

| 项目 | 固定值 |
| --- | --- |
| 原始数据 | [`Skylion007/openwebtext`](https://huggingface.co/datasets/Skylion007/openwebtext) |
| 数据 revision | `79d93d7` |
| 预处理 | [nanoGPT `data/openwebtext/prepare.py`](https://github.com/karpathy/nanoGPT/blob/master/data/openwebtext/prepare.py) |
| nanoGPT commit | `3adf61e` |
| 划分 | `train_test_split(test_size=0.0005, seed=2357, shuffle=True)` |
| Train | 8,009,762 documents；约 9,035,582,198 GPT-2 tokens |
| Validation | 4,007 documents；约 4,434,897 GPT-2 tokens |
| 编码 | GPT-2 BPE；每个文档末尾追加 token `50256`；`uint16` |
| 外部分布检查 | [`Salesforce/wikitext`](https://huggingface.co/datasets/Salesforce/wikitext) `wikitext-103-v1` validation，只读 |

预处理直接执行 nanoGPT 现成脚本，只将数据 ID、revision 和输出目录改为冻结值。脚本自动完成 split、tokenization 与二进制写入，不增加语料清洗、重写、去重或人工筛选。

生成后的数据基线固定为：

```text
train.bin
├── GPT-2 reference loss：只读评估
├── MHA：继续预训练对照
├── GQA-8：读取同一训练位置
└── GQA-4：读取同一训练位置

val.bin
└── 全部完整 validation blocks：只读，用于 checkpoint 选择与恢复曲线
```

训练使用 `sequence_length=1024`、`global_batch_tokens=524,288`，数据顺序固定为 `train.bin` 的连续 token 顺序。三个评估位置由更新步数唯一确定：

| 位置 | Update | 累计训练 tokens |
| --- | ---: | ---: |
| 短程检查 | 20 | 10,485,760 |
| 中程检查 | 96 | 50,331,648 |
| 最终检查 | 192 | 100,663,296 |

Validation 读取 `val.bin` 前 4,433,920 个可预测 tokens，共 4,330 个连续 1024-token blocks，尾部不足一个 block 的 976 tokens 丢弃。所有 checkpoint 使用完全相同的 validation blocks。

nanoGPT 报告 GPT-2 Medium 在其 OpenWebText validation 上的 loss 约为 2.84。该数值作为数据与 loss 实现的 sanity reference，不作为硬性复现阈值。GPT-2 的原始 WebText 没有公开，专题结论限定在 OpenWebText 恢复训练分布。WikiText-103 只在 G0、G3、G5 和 G7 的最终 checkpoint 上运行，用于观察外部分布变化，不参与模型选择。

#### OLMo 3 从头预训练

| 项目 | 固定来源 |
| --- | --- |
| 数据仓库 | [`allenai/DataDecide-data-recipes`](https://huggingface.co/datasets/allenai/DataDecide-data-recipes) |
| 数据配方 | `fineweb-edu-dedup/v0` |
| 预分词路径 | `preprocessed/fineweb-edu-dedup/v0/gpt-neox-olmo-dolma-v1_5/` |
| Tokenizer | [`allenai/gpt-neox-olmo-dolma-v1_5`](https://huggingface.co/allenai/gpt-neox-olmo-dolma-v1_5) |
| 数据格式 | OLMo-core 可直接读取的预分词 `.npy` shards |

数据仓库 revision 固定为 `3baf34b`。训练 manifest 按下表顺序连接，不再在运行时选择文件：

| 顺序 | 训练文件 | Token 数 |
| ---: | --- | ---: |
| 1 | `part-000-00000.npy` | 1,073,741,783 |
| 2 | `part-000-00001.npy` | 506,924,995 |
| 3 | `part-001-00000.npy` | 1,073,740,304 |
| 4 | `part-001-00001.npy` | 610,967,463 |
| 5 | `part-002-00000.npy` | 797,578,124 |
|  | **可用训练 tokens** | **4,062,952,669** |

Validation 固定使用同目录下 `part-003-00000.npy` 的前 4,194,304 tokens，共 2,048 个连续 2048-token blocks。该文件不进入任何训练 manifest，其余 token 不参与 validation，也不用于模型选择。

数据加载统一采用 OLMo-core `NumpyFSLDataset` 的 concatenate-and-chunk 路线：

```text
sequence_length = 2048
global_batch_tokens = 524,288
data_loader_seed = 2027
shuffle = true
epoch = 1
```

OLMo-core 根据 dataset fingerprint、seed 和 epoch 生成并保存 global indices。所有 MHA / GQA / MQA 运行复用相同的 indices 文件；模型初始化 seed 单独记录为 `42` 或 `31415`，并使用阶段 B1 冻结的配对初始化策略。恢复训练必须同时恢复 data-loader state。

三种规模使用同一数据基线和明确的更新预算：

| 用途 | Updates | 累计训练 tokens | 评估 updates |
| --- | ---: | ---: | --- |
| 100M 冒烟模型 | 20 | 10,485,760 | 0、10、20 |
| 190M 候选筛选 | 960 | 503,316,480 | 0、240、480、720、960 |
| 370M 正式验证 | 7,680 | 4,026,531,840 | 0、960、1,920、3,840、5,760、7,680 |

开发与候选选择只使用固定 validation loss。正式下游评估固定采用 [OLMES](https://github.com/allenai/olmes) commit `5a51f50` 的两个 suite：

```text
core_9mcqa::olmes
mmlu::olmes
```

这两个 suite 合并构成 DataDecide 使用的 OLMES 标准 10 项多项选择评估。190M 先按固定 validation loss 冻结胜出比例，再只评估 MHA baseline 与胜出比例的最终 checkpoint；370M 评估四个正式运行的最终 checkpoint。中间 checkpoint 只计算固定 validation loss。任务数据、few-shot 配置和 metric 由固定 OLMES commit 提供，不在本项目中修改。

DataDecide 入口已经完成 FineWeb-Edu 的质量筛选、去重和预分词。学习者只下载上述六个文件并核对 Hub revision、LFS hash、文件字节数和 token 数。项目不下载原始网页、不重跑过滤器、不重新分词。

### 2.4　固定工具链

| 环节 | 固定工具 | 使用边界 |
| --- | --- | --- |
| GPT-2 参考实现 | 冻结版本的 Transformers | 加载官方权重、Tokenizer 与参考输出；作为本地薄改造的结构来源 |
| GPT-2 数据准备 | nanoGPT `openwebtext/prepare.py` | 只固定数据 ID、revision 和输出目录，不另写预处理器 |
| attention kernel | PyTorch SDPA | 复用官方 kernel，不编写 CUDA / Triton kernel |
| GPT-2 恢复训练 | Accelerate + 本地薄改模型 | 复用既有训练能力，只维护薄入口和必要转换函数 |
| OLMo 3 训练 | OLMo-core | 复用官方 config、data loader、trainer、checkpoint 与 callback |
| OLMo 数据 | DataDecide 预分词 shards | 不建立语料清洗或 tokenization pipeline |
| 下游评估 | OLMES `core_9mcqa::olmes` + `mmlu::olmes` | 固定 OLMES commit，直接使用 DataDecide 口径 |
| 曲线与系统记录 | TensorBoard + PyTorch profiler 的短窗口 | 不搭建实验数据库或长期 profiling 服务 |

首次试运行时冻结 nanoGPT、Hugging Face Datasets、tiktoken、OLMo-core、OLMES、PyTorch、Transformers、CUDA 和 attention backend。每次正式运行导出 resolved config。

### 2.5　最小代码边界

GPT-2 子项目只维护：

```text
一个基于冻结 Transformers 源码的 GPT-2 Medium 本地薄改版本
一个 Hugging Face state dict 映射与校验函数
一个 MHA-to-GQA K/V 聚合函数
一个复用现成训练能力的连续预训练薄入口
一组 correctness tests
一个统一 benchmark 入口
```

本地版本保留官方 embedding、位置编码、LayerNorm、MLP、LM head、模块层级和参数命名。独立修改限定在 attention 调用路径、基础动态 KV Cache、KV-head 配置和权重聚合。专题不从空文件重新设计完整 GPT-2，也不扩展 Static Cache、量化缓存、offload、paged attention 或服务框架缓存。连续预训练入口负责数据接入和配置，不另写通用训练循环。

OLMo 3 子项目的训练入口只维护官方 recipe 的配置覆盖：

```text
model preset
n_kv_heads
data manifest
token budget / seed
launcher configuration
```

此外只维护一个初始化配对审计。审计首先读取官方模型的参数布局并比较共享逻辑参数；只有同 seed 仍导致共享参数不同，才使用一个薄对齐函数复制 embedding、norm、MLP、LM head 以及逻辑上的 Q/O 参数，K/V 参数继续采用官方初始化方法。该函数不改变模型结构或初始化分布，不扩展为通用 checkpoint 转换框架。

专题不增加通用训练框架、数据平台、模型仓库、任务调度器、checkpoint 数据库或可视化前端。

---

## 三、时间安排

### 3.1　相对研究周总览

| 周次 | 核心工作 | 主要交付物 |
| ---: | --- | --- |
| R1 | GPT-2 兼容实现、权重加载与参考对齐 | state dict 映射、logits / loss / generation 测试 |
| R2 | SDPA、KV Cache 与推理 benchmark | cache correctness、prefill / decode 基线 |
| R3 | GQA 转换与 100,663,296-token 同预算训练 | MHA 对照、GQA-8 / GQA-4 恢复曲线和效率对比 |
| R4 | OLMo-core、DataDecide 与 100M 冒烟测试，启动 190M 筛选 | 冻结环境、manifest、resolved config、恢复测试 |
| R5 | 完成 OLMo 3 190M 单 seed 筛选，启动 370M 正式验证 | MHA、GQA-2、GQA-4、MQA 筛选表 |
| R6～R7 | OLMo 3 370M 双 seed 正式验证 | 正式对照、质量与效率联合分析 |
| R7～R7.5 | 关键入口复现与结项 | 复现记录、分析 notebook、专题总结 |

若本草案被重新冻结并执行，关键训练提前完成时，阶段 B4 可以在 R7 前启动。R1 从阶段 11 结项后的第一个完整研究周开始，相对周次避免前序阶段变化导致整份计划反复改期。

### 3.2　投入预算

| 类型 | 核心总投入（含复习） |
| --- | ---: |
| 必要学习与实验设计 | 25～32 小时 |
| GPT-2 实现、测试与迁移 | 58～75 小时 |
| OLMo-core 配置与冒烟测试 | 18～25 小时 |
| 正式训练、评估与运行检查 | 62～82 小时 |
| 分析、复现与结项 | 30～41 小时 |
| **合计** | **193～255 小时** |

训练矩阵在启动前一次性生成配置，使用服务器调度能力并行执行。日常人工检查集中在启动后短窗口、固定 evaluation checkpoint 和训练结束三个节点。

---

## 四、阶段执行计划

### 阶段 A1　GPT-2 兼容复现

> **计划窗口**　R1
>
> **预计总投入**　25～31 小时，其中首次学习与执行 19～24 小时，第三天复习 6～7 小时
>
> **阶段目标**　建立可以承接官方 checkpoint 的本地实现，并证明基线等价。

#### A1.1　实现范围

从冻结版本的 Transformers GPT-2 源码派生本地薄改版本，保留 embedding、learned positional embedding、pre-LayerNorm block、GELU MLP、tied LM head、模块层级和参数命名。首先在未改变结构时完成权重加载与数值对齐；后续自定义代码集中在 attention、基础动态 KV Cache 和 GQA 权重转换。官方实现始终作为只读 reference，本阶段不重新设计已经由 TinyGPT 与 MiniMind 覆盖的通用组件。

#### A1.2　正确性测试

在 FP32、`model.eval()`、固定输入下比较：

- 关键层中间张量；
- final logits 的 max / mean absolute error 与 cosine similarity；
- causal LM loss；
- greedy generation token sequence。

偏差超过冻结容差时停留在本阶段。后续结构实验不得在未对齐的实现上启动。

#### A1.3　阶段交付物

- 兼容模型与 state dict 映射函数
- 固定测试输入和参考输出
- logits、loss 与 greedy generation 对齐报告
- 官方配置、权重 revision 与环境清单

#### A1.4　验收标准

- 所有官方参数均有明确映射或有记录的处理方式；
- FP32 参考测试通过冻结容差；
- checkpoint 保存、重载后的输出保持一致。

### 阶段 A2　SDPA 与 KV Cache

> **计划窗口**　R2
>
> **预计总投入**　25～31 小时，其中首次学习与执行 19～24 小时，第三天复习 6～7 小时
>
> **阶段目标**　理解 attention kernel 与增量解码的数据流，并建立可信的系统指标基线。

#### A2.1　SDPA 替换

使用 `torch.nn.functional.scaled_dot_product_attention` 替换显式 attention matrix 实现。训练与评估分别显式传入正确的 dropout 概率，记录实际选择的 SDPA backend。

PyTorch 的 GQA 支持带有 query-head 与 KV-head 整除约束，相关接口仍标记为实验特性。专题对使用的 PyTorch 版本执行 correctness test，不依赖跨版本数值完全一致。

GQA 正式 benchmark 必须使用实际支持不同 query/KV head 数的执行路径，例如 PyTorch SDPA 的 `enable_gqa=True` 或等价原生 backend。运行前记录 Q/K/V shape、实际 backend 和 profiler 摘要，确认 cache 只保存 `n_kv_heads` 个 K/V heads。若 backend 在 attention 前长期物化为 query-head 数，质量结果仍可保留，系统效率结论只标注为该 fallback 的测量结果，不据此否定 GQA 的缓存收益。

#### A2.2　KV Cache

实现每层 K/V cache 的创建、追加和读取，分别处理 prefill 与 decode。带 cache 的非方形 attention 显式检查 causal mask；单 token decode、padding batch 和满长度边界分别测试。

固定比较：

- cache on / off 的 greedy generation；
- prefill length 128、512、1024；
- decode context 128、512、1000；
- batch size 1、4、8；
- TTFT、TPOT、prefill/decode tokens/s、peak VRAM 与理论 KV Cache bytes/token。

TTFT 与 TPOT 沿用 [MLPerf Inference](https://mlcommons.org/2024/03/mlperf-llama2-70b/) 的阶段划分：TTFT 覆盖 prompt 处理至首 token，TPOT 统计首 token 之后的平均逐 token 时间。本专题只做单请求和固定 batch 的模型级 benchmark，不搭建服务压测系统。

#### A2.3　阶段交付物

- SDPA 与 KV Cache 实现
- cache correctness tests
- 统一 benchmark 脚本与硬件信息
- GQA backend、Q/K/V shape 与 cache layout 审计
- MHA 系统指标基线

#### A2.4　验收标准

- cache on / off 在冻结测试上生成相同 token；
- SDPA 版本通过 logits 与 loss 容差；
- cache 中 K/V head 数与理论配置一致，正式 GQA benchmark 的 backend 已明确；
- benchmark 包含 warm-up、同步、重复次数、dtype 和 backend。

### 阶段 A3　GPT-2 MHA-to-GQA 迁移

> **计划窗口**　R3
>
> **预计总投入**　26～32.5 小时，其中首次学习与执行 20～25 小时，第三天复习 6～7.5 小时
>
> **阶段目标**　测量权重转换的即时损失、短程恢复和推理收益。

#### A3.1　权重转换

保留 Q projection 与 output projection。K/V heads 按连续分组求均值：

| 变体 | Query heads | KV heads | 聚合方式 |
| --- | ---: | ---: | --- |
| MHA | 16 | 16 | 原权重 |
| GQA-8 | 16 | 8 | 每 2 个 K/V heads 求均值 |
| GQA-4 | 16 | 4 | 每 4 个 K/V heads 求均值 |

该方法依据 [GQA 论文](https://aclanthology.org/2023.emnlp-main.298/) 的 checkpoint conversion 与 uptraining 思路。转换后立即评估，不先用训练掩盖实现错误。

#### A3.2　恢复训练

MHA、GQA-8 与 GQA-4 各执行一次 192-update 连续预训练，每组总量为 100,663,296 tokens。MHA 运行用于区分 OpenWebText 继续适应与 GQA 架构恢复。10,485,760、50,331,648 和 100,663,296 tokens 是同一运行中的 evaluation checkpoints，不拆成三次独立训练。

固定条件包括：

- 相同数据和样本顺序；
- context length 1024；
- 相同 optimizer、learning-rate schedule 与 effective batch tokens；
- BF16 训练，FP32 reference tests；
- 相同 evaluation windows 与 generation config；
- 全参数训练。

学习率先通过两组短程 LR range check 确认，再冻结一个值。核心实验不执行超参数网格。

#### A3.3　核心矩阵

| 编号 | 模型 | 训练 | 研究作用 |
| --- | --- | --- | --- |
| G0 | 官方 GPT-2 Medium | 无 | 官方质量基线 |
| G1 | 本地兼容 MHA | 无 | 实现正确性 |
| G2 | MHA + SDPA + Cache | 无 | 系统基线 |
| G3 | MHA uptrained | 100,663,296 tokens | 数据适应与训练对照 |
| G4 | GQA-8 转换后 | 无 | 即时迁移损失 |
| G5 | GQA-8 uptrained | 100,663,296 tokens | 恢复与效率 |
| G6 | GQA-4 转换后 | 无 | 更高压缩下的即时损失 |
| G7 | GQA-4 uptrained | 100,663,296 tokens | 恢复与效率 |

#### A3.4　验收标准

- 两种 GQA checkpoint 均能保存、重载和生成；
- update 0、20、96、192 四个位置具有同口径评估；
- MHA、GQA-8 与 GQA-4 使用相同数据位置、训练预算和评估窗口；
- 质量变化能够与理论和实测 KV Cache 变化共同解释；
- 结论包含数据分布和 100,663,296-token budget 的限制。

### 阶段 B1　OLMo 3 最小闭环

> **计划窗口**　R4
>
> **预计总投入**　18～25 小时，其中首次学习与执行 14～19 小时，第三天复习 4～6 小时
>
> **阶段目标**　以 OLMo 3 100M 官方预设验证环境、数据、GQA 配置、checkpoint 与恢复训练。

#### B1.1　固定入口

从 OLMo-core 官方训练示例派生一份最小 recipe，只修改模型预设、`n_kv_heads`、数据路径、token budget 和运行环境字段。数据直接读取 DataDecide `.npy` shards。

#### B1.2　最小测试

- 临时设置 `shuffle=false`，使用训练 manifest 最前面的 256 个固定 sequence 完成过拟合检查；正式冒烟训练恢复冻结的 data-loader 配置；
- MHA、GQA-4 各完成 forward / backward；
- 使用 seed `42` 构建 MHA、GQA-2、GQA-4 与 MQA，完成共享逻辑参数的初始化配对审计；必要时生成一次配对初始化 checkpoint 集合；
- 记录实际 attention backend、Q/K/V shape 与 cache layout，确认 GQA 路径没有在 cache 中复制 K/V heads；
- MHA 完成 20 updates、10,485,760 tokens 的冒烟训练；
- 在 update 10 保存 checkpoint，并从下一 step 恢复至 update 20；
- 单机短跑通过后再启动多卡；
- 导出 resolved config 与参数统计；
- 验证 train / validation manifest 无文件交集。

#### B1.3　验收标准

- 官方 100M 预设无需结构补丁即可训练；
- `n_kv_heads` 变更后 shape、loss 和 checkpoint 均正常；
- 初始化审计能够证明共享参数相同，或已经通过冻结的薄对齐函数生成配对初始化；
- 正式 benchmark 使用的 GQA backend 与 cache layout 已确认；
- 20-update 冒烟训练使用冻结的训练与 validation 数据；
- 中断恢复后的 step、data position 和学习率连续；
- 正式矩阵只依赖配置覆盖和冻结的配对初始化输入，不需要模型结构补丁。

### 阶段 B2　OLMo 3 190M 候选筛选

> **计划窗口**　R4 后半～R5
>
> **预计总投入**　27～34 小时，其中首次学习与执行 21～26 小时，第三天复习 6～8 小时
>
> **阶段目标**　在相同 503,316,480-token budget 下筛选可进入正式验证的 GQA 比例。

#### B2.1　筛选矩阵

| 编号 | 分组比例 | Query heads | KV heads | Seed |
| --- | --- | ---: | ---: | ---: |
| O0 | MHA | 12 | 12 | 42 |
| O1 | GQA-2 | 12 | 6 | 42 |
| O2 | GQA-4 | 12 | 3 | 42 |
| O3 | MQA | 12 | 1 | 42 |

四组任务从同一配置模板和 seed `42` 的配对初始化集合生成并并行启动。190M 只承担方案筛选，单 seed 结果不形成稳健性结论。

每组执行 960 updates、503,316,480 tokens，并在 update 0、240、480、720、960 上计算固定 validation loss。胜出比例冻结后，OLMES 只运行 MHA baseline 与胜出比例的 update 960 checkpoint。

#### B2.2　筛选规则

候选规则在四组正式训练启动前冻结。令 $L_{\mathrm{MHA}}$ 为 O0 在 update 960 的
validation loss，候选模型的 loss 为 $L_{\mathrm{candidate}}$，质量容差固定为
$\Delta L_{\max}=0.02$。该数值是本学习项目用于筛选的预注册 guardrail，不解释为通用模型质量阈值。

候选首先必须满足：

- 没有 loss spike、NaN / Inf 或异常 gradient norm；
- $L_{\mathrm{candidate}}-L_{\mathrm{MHA}}\le 0.02$；
- 正式 GQA backend、cache layout 和 benchmark correctness test 通过。

在合格候选中选择 KV heads 最少的方案进入 370M；同时报告 tokens/s、step time、peak VRAM、TTFT、TPOT、decode throughput 与 KV Cache。若没有 GQA 通过质量门槛，则选择 validation loss 最低的 GQA 继续完成方法验证，并明确记录“未达到预注册质量门槛”。

胜出比例冻结后，对 MHA baseline 与胜出比例运行固定 OLMES 10 任务，作为下游退化的辅助检查。OLMES 不反向参与 190M 候选选择，也不扩展为四组完整评估矩阵。

进入 370M 的候选固定为 MHA baseline 和一个最佳 GQA 比例。MQA 可以胜出，不预设 GQA-4 的结果。

#### B2.3　验收标准

- 四组运行使用相同训练 token 数、数据顺序和评估位置；
- 每组均保存 resolved config、最终 checkpoint 和统一 validation / benchmark JSON；
- MHA baseline 与胜出比例保存 OLMES 结果 JSON；
- `delta_loss_max`、候选资格和选择结果均保存于筛选记录；
- 190M 结果明确标注为单 seed screening。

### 阶段 B3　OLMo 3 370M 正式验证

> **计划窗口**　R5 后半～R7
>
> **预计总投入**　56～70 小时，其中首次学习与执行 43～54 小时，第三天复习 13～16 小时
>
> **阶段目标**　验证 190M 胜出比例在更大官方预设上的质量—效率权衡。

#### B3.1　正式矩阵

| 组别 | 架构 | Token budget | Seed |
| --- | --- | ---: | --- |
| F0 | MHA baseline | 4,026,531,840 | 42 |
| F1 | 最佳 GQA | 4,026,531,840 | 42 |
| F2 | MHA baseline | 4,026,531,840 | 31415 |
| F3 | 最佳 GQA | 4,026,531,840 | 31415 |

最佳比例映射到 370M 对应的 KV-head 数。例如 GQA-4 在 190M 使用 3 个 KV heads，在 370M 使用 4 个 KV heads。每个 seed 分别生成一组配对初始化 checkpoint，baseline 与候选共享所有逻辑上同形的非 K/V 参数。

每组执行 7,680 updates、4,026,531,840 tokens，并在 update 0、960、1,920、3,840、5,760、7,680 上计算固定 validation loss。OLMES 只运行 update 7,680 的最终 checkpoint。

#### B3.2　比较方式

核心训练按 token-matched 设计。compute 视角直接复用同一次运行的 wall-clock、GPU time、throughput 和中间 checkpoint，绘制 validation loss 对 tokens 与 validation loss 对 GPU time 两组曲线。核心计划不再启动一套独立的 compute-matched 训练矩阵。

报告至少包括：

- 两个 seed 的最终 validation loss 和差值；
- 训练曲线、gradient norm、step time 与 tokens/s；
- prefill、decode、peak VRAM 与 KV Cache；
- 固定 OLMES 10 任务结果；
- 当前规模、数据配方和 4,026,531,840-token 区间下的结论边界。

#### B3.3　验收标准

- baseline 与候选各完成两个 seed，或留下由预设停止规则产生的记录；
- 质量、训练效率和推理效率使用同一组 checkpoint 统计；
- 结果能够区分平均趋势、seed 波动和系统测量噪声；
- 实验结论不外推到未测试的 7B 以上模型。

### 阶段 B4　复现与结项

> **计划窗口**　R7～R7.5；前序阶段提前完成时可以在 R7 前启动
>
> **预计总投入**　16～31 小时，其中首次学习与执行 12～24 小时，第三天复习 4～7 小时
>
> **阶段目标**　形成可复现入口和一份有边界的研究总结。

只复现两个关键入口：GPT-2 的转换后即时评估，以及 OLMo 3 370M 的一个短 checkpoint 恢复段。复现结果达到冻结容差后停止，不重新执行全矩阵。

最终使用一个 notebook 读取 TensorBoard 导出值、OLMo evaluator JSON 和 benchmark JSON。分析 notebook 不承担训练调度、数据转换或结果数据库功能。

#### B4.1　阶段交付物

- GPT-2 转换后即时评估的复现记录
- OLMo 3 370M 短 checkpoint 恢复记录
- 统一分析 notebook 与专题总结
- 配置、manifest、checkpoint 和结果文件索引

#### B4.2　验收标准

- 两个关键入口均能从冻结配置重新启动；
- 复现结果达到各自前序阶段冻结的正确性与恢复连续性口径；
- 总结能够区分迁移损失、架构差异、seed 波动与系统测量噪声；
- 结项未引入新的训练矩阵或结果管理系统。

---

## 五、实验与评估规范

### 5.1　核心指标

| 维度 | 指标 | 说明 |
| --- | --- | --- |
| 质量 | validation loss、perplexity | 核心选择指标 |
| 迁移 | KL divergence、恢复比例 | 仅用于 GPT-2 checkpoint 迁移 |
| 下游 | 固定 OLMES 10 任务 | `core_9mcqa::olmes` + `mmlu::olmes`，作为辅助信号 |
| 训练 | tokens/s、step time、peak VRAM | 固定硬件、dtype 与 backend |
| 推理 | TTFT、TPOT、prefill/decode tokens/s | 固定 batch、context 与输出长度 |
| 缓存 | 实测显存、理论 bytes/token | 同时报告框架开销与理论值 |
| 稳定性 | gradient norm、loss spike、NaN / Inf | 使用现成 callback 记录 |

### 5.2　必须固定的条件

同一子实验至少固定：

- 代码 commit 与依赖锁定；
- 模型预设及除 `n_kv_heads` 外的结构字段；
- Tokenizer、数据 revision、manifest 与 data-loader seed；
- training tokens、sequence length 与 effective batch tokens；
- optimizer、learning-rate schedule、warm-up 与 gradient clipping；
- precision、attention backend、并行策略和硬件型号；
- 配对初始化策略、初始化审计报告与实际 Q/K/V shape；
- KV Cache layout、实际 K/V head 数与 GQA backend；
- evaluation checkpoints、任务版本与 generation config；
- benchmark warm-up、同步方式、重复次数与统计量。

### 5.3　最小实验记录

每次运行只保存必要字段：

```text
run_id
git_commit
dependency_lock
model_preset / n_kv_heads / parameter_count
model_and_data_revision
dataset_fingerprint / data_manifest_hash
global_indices_path
seed / token_budget / sequence_length
optimizer_and_lr_schedule
hardware / precision / attention_backend
initialization_policy / initialization_report_path
qkv_shape / kv_cache_layout
checkpoint_path
evaluation_json_path
benchmark_json_path
status / stop_reason
```

数据文件自身由 Hugging Face revision 与 LFS hash 标识。`dataset_fingerprint` 和 global indices 路径直接读取 OLMo-core 已有输出；manifest hash 使用通用命令自动生成。项目不编写额外的指纹逻辑，也不另建数据版本系统。

### 5.4　停止规则

出现以下情况时停止单次运行并修复原因：

- loss、gradient 或参数出现 NaN / Inf；
- 数据位置、学习率或 step 在 checkpoint 恢复后不连续；
- 不同架构读取了不同数据顺序；
- 运行配置与导出的 resolved config 不一致；
- correctness test 未通过。

190M screening 的四组核心运行统一完成 960 updates，并按阶段 B2 冻结的 update 960 规则筛选。370M 正式运行依照预注册 budget 完成，除非出现训练错误或明确的灾难性退化。

---

## 六、项目里程碑

### 里程碑一　GPT-2 基线可信

- 官方 checkpoint 已成功映射；
- logits、loss 与 greedy generation 通过对齐测试；
- SDPA 与 KV Cache 具有 correctness tests 和统一 benchmark。

### 里程碑二　Checkpoint 迁移闭环

- GQA-8 与 GQA-4 均完成转换；
- 一条 MHA 对照与两条 GQA 的 100,663,296-token 训练完成；
- 即时损失、恢复曲线与推理收益已统一分析。

### 里程碑三　OLMo 3 候选冻结

- 100M 冒烟测试与 checkpoint 恢复通过；
- 初始化配对与 GQA backend/cache 审计通过；
- 190M 四组 screening 完成；
- 最佳 GQA 比例和选择依据在正式实验前冻结。

### 里程碑四　正式架构结论

- 370M baseline 与最佳 GQA 完成双 seed 验证；
- token 与 GPU 时间两种视角均已报告；
- 复现入口、配置、manifest、结果 JSON 和总结齐全。

---

## 七、条件式扩展

以下内容均不影响核心计划验收：

### 7.1　第二候选比例

只有两个通过质量门槛的 190M 候选 final validation loss 差不超过 `0.01` 时，增加一个 370M 第二候选，先执行单 seed。

### 7.2　后训练迁移

只有 Base 结论清晰且仍有兴趣时，对 370M MHA baseline 和最佳 GQA 使用同一份 [`allenai/tulu-3-sft-mixture`](https://huggingface.co/datasets/allenai/tulu-3-sft-mixture) 固定子集各训练一次，检查架构差异是否延续。数据 revision 固定为 `b14afda`，训练入口固定为 [allenai/open-instruct](https://github.com/allenai/open-instruct) commit `b18cfc5`；两组使用同一确定性样本索引与 Tülu chat template，不构造、清洗或改写标签。

### 7.3　更长上下文

只有核心 context 下 GQA 质量接近 MHA 时，再增加一个长上下文推理 benchmark。输入不另找数据集，直接从固定 DataDecide validation shard `part-003-00000.npy` 的 token offset `4,194,304` 之后截取同一组连续 token IDs；context length 限于模型实际支持范围，并在第一次运行前冻结。该扩展只评估 prefill、decode、显存和 KV Cache，不重新训练模型，也不形成质量结论。

### 7.4　其他架构变量

QK-Norm、FFN ratio、MoE 和新的位置编码各自属于独立研究题。每次只选择一个变量，并从本专题冻结的 baseline 开始。

---

## 八、结项标准

满足以下条件后，本专题结束：

- 能够解释官方 checkpoint 到本地实现的参数映射与数值对齐方法；
- 能够解释 SDPA、causal mask、KV Cache 与增量解码的关系；
- 能够完成 MHA-to-GQA 权重聚合和固定 budget 恢复训练；
- 能够使用同预算 MHA 对照区分数据适应与架构恢复；
- 能够使用 OLMo 3 官方预设和 DataDecide 预分词数据完成从头预训练；
- 能够设计只改变 `n_kv_heads` 的受控实验；
- 能够检查初始化配对、实际 attention backend 与 KV Cache layout；
- 能够从质量、训练效率和推理效率三个维度解释 GQA trade-off；
- 能够区分 screening 结果、双 seed 正式结果和未验证外推；
- 核心配置、数据 manifest、checkpoint 索引、结果 JSON 和分析 notebook 可以复核。

最终产出包含两个子项目：

```text
Checkpoint Migration from Multi-Head Attention to Grouped-Query Attention in GPT-2 Medium

Quality–Efficiency Trade-offs of Grouped-Query Attention in Small OLMo 3 Models
```

完成上述闭环后，不因增加更多模型、数据配方、随机种子或架构变量延迟结项。
